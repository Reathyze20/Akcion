# Audit Akcion — 22. 8. 2026

> **Co to je.** Strojový audit celé codebase proti tomu, k čemu má appka sloužit: sbírat v reálném
> čase informace z internetu, radit kolik koupit/prodat a kdy, a bránit majiteli v emočních chybách.
> Sedm paralelních agentů mapovalo po jedné oblasti, každý nález pak dostal nezávislého agenta
> s úkolem ho **vyvrátit**. Přežilo jen to, co vyvrátit nešlo.

## Jak to číst — a čemu nevěřit

- **106 nálezů** celkem, z toho **56 potvrzených**, **19 vyvrácených**, **31 neověřených**.
- Vyvráceno bylo **25 %** z ověřených. To je míra falešných poplachů surových nálezů —
  a přesně proto sekci *Neověřeno* nelze brát jako fakta. Než podle ní něco změníš, ověř to.
- Každý potvrzený nález má kotvu `soubor:řádek`. Kotvy platí ke commitu `8e32c1d` (větev `feature/gomes-fidelity`).
- **Cena auditu: 104 agentů, 18 MB transkriptů, ~22 minut.** Neúměrně mnoho — fan-out neměl strop
  a agenti opakovaně načítali `InvestmentTerminal.tsx` (261 KB). Příště: jeden agent na oblast, strop na počet verifikací.

## Skóre podle oblastí

| Oblast | Verdikt | Nálezů | Potvrzeno |
|---|---|---|---|
| Emoční brzdy | `WORKS_BUT_UNREACHABLE` | 13 | 7 |
| Časování a automatika | `STUB_OR_FAKE` | 9 | 7 |
| Tok „Analyzovat“ | `STUB_OR_FAKE` | 12 | 8 |
| Provozuschopnost a důvěra | `PARTIAL` | 10 | 8 |
| Zdroje dat z internetu | `PARTIAL` | 19 | 12 |
| Kolik koupit / prodat | `PARTIAL` | 15 | 10 |
| Poctivost čísel a čerstvost dat | `PARTIAL` | 28 | 4 |

**Potvrzené nálezy podle závažnosti:** P0 — ohrožuje peníze: **8** · P1 — blokuje misi: **17** · P2 — tření: **21** · P3 — kosmetika: **10**

---

## Potvrzené nálezy

Každý z nich prošel agentem, jehož úkolem bylo ho zabít. Nezabil.

### P0 — ohrožuje peníze

**1. Green/Red lines are fabricated from spot price when absent, and the "estimated" provenance is discarded — with three corrections to the reporter's specifics. (1) The cited prompt block at prompts_ente …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/core/prompts_enterprise_v2.py:130-152 instructs the model: `4. **Default:** green_line = current_price * 0.80 (20% pod aktuální cenou)` / `4. **Default:** red_line = current_price * 1.50 (50% nad aktuální cenou pro growth stock)` / `3. **Default:** grey_line = current_price * 0.65` and then `**DŮLEŽITÉ:** … - NIKDY nenechávej null - vždy poskytni odhad` ("NEVER leave null — always provide an estimate"). Repeated at :516 `- NIKDY neponechávej null - vždy poskytni hodnotu`. The backend then does it a second time in code: backend/app/services/gomes_deep_dd.py:306-321 `green_line = round(current_price * 0.80, 2); price_lines_estimated = True` / `red_line = round(current_price * 1.50, …
- **Dopad na misi:** Directly violates the non-negotiable "never fake numbers filling gaps" and breaks (2) how much to buy/sell and (5) mechanical not vibes. A green_line of price×0.80 tells him a stock is a buy at a level that has no valuation basis whatsoever. It is not cosmetic: TradingDeck.tsx:95-101 makes the fabricated Red Line a CRI …
- **Návrh opravy:** Delete the ±% defaults from prompts_enterprise_v2.py:130-152 and :513-517 and from gomes_deep_dd.py:306-321; let green_line/red_line be null. Add `price_lines_estimated: bool` and `data_gaps: list[str]` to DeepDueDiligenceResult, persist them on Stock, and render "⚠️ odhad — není z transkriptu" next to any estimated li …
- **Ověřovatel:** I tried to break this five ways and the core survived every one.

WHAT I CONFIRMED VERBATIM:
1. backend/app/services/gomes_deep_dd.py:303-321 — the fallback is exactly as cited: `green_line = round(current_price * 0.80, 2)`, `red_line = round(current_price * 1.50, 2)`, `grey_line = round(current_price * 0.65, 2)`, each setting `price_lines_estimated = True`.
2. The flag genuine …

**2. LIVE: market_data.py:563 stamps `position.last_price_update = datetime.utcnow()` on every price refresh whose returned dict has a truthy `current_price`, without checking whether that price was actual …**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/services/yahoo_cache.py:132-134 — `if not success:\n    logger.warning(f"Failed to refresh {ticker}, returning stale cache")\n    return cached  # Return stale data rather than None` (returned with no staleness marker; the caller cannot tell). Caller: backend/app/services/market_data.py:560-563 — `for position in positions:\n    if position.ticker == ticker:\n        position.current_price = price\n        position.last_price_update = datetime.utcnow()`. The same happens on an ordinary cache hit: yahoo_cache.py:136-138 returns `cached` unchanged when `_determine_refresh_needs` says no refresh, and backend/app/core/market_hours.py:219-222 allows a quote up to 12h old (`if age_hour …
- **Dopad na misi:** Directly defeats the non-negotiable 'must survive weeks of absence with honest stale-data labelling'. If Yahoo is down (or the ticker delists) for a week while he is in an MS relapse, every refresh re-stamps the same week-old price as 'updated now', so `⚠️ STARÁ CENA` (daily_actions.py:182) never fires and the daily ac …
- **Návrh opravy:** Have `get_stock_data` return the cache's own `market_data_updated` alongside the price (it already reads it — yahoo_cache.py:208). In market_data.py set `position.last_price_update = <that timestamp>`, never `utcnow()`.
- **Ověřovatel:** Every cited line is present and unchanged. yahoo_cache.py:132-134 returns `cached` verbatim on refresh failure; market_data.py:560-563 stamps `position.last_price_update = datetime.utcnow()` whenever `data.get("current_price")` is truthy, with no distinction between a fresh quote and a stale-cache fallback. daily_actions.py:42/:142 and routes/daily_actions.py:66 confirm `last_p …

**3. A failed Yahoo market fetch is swallowed by a nested `except` in `_fetch_and_cache_data` (yahoo_cache.py:337-401), so the function still logs `success=True` and returns True (:415-428). The upsert lea …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** yahoo_cache.py:340-365 wraps the market fetch in its own try: `info = stock.info` / `fast_info = stock.fast_info` / `"current_price": fast_info.get("lastPrice")` ... `except Exception as e: logger.warning(f"Failed to fetch market data for {ticker}: {e}")`. Execution then falls straight through to `_upsert_cache(data)` and `yahoo_cache.py:434  return True`, after `yahoo_cache.py:433  logger.info(f"Successfully fetched and cached {ticker}")` and `_log_refresh(..., success=True)` (:429). Because success is True, get_stock_data never takes the honest branch at yahoo_cache.py:132-134 (`if not success: logger.warning(f"Failed to refresh {ticker}, returning stale cache"); return cached`) — it reloa …
- **Dopad na misi:** Breaks (5) mechanical-not-vibes and the non-negotiable "survive weeks of absence with honest stale-data labelling". services/daily_actions.py:142 `elif now - pos.last_price_update > STALE_PRICE_AFTER` (STALE_PRICE_AFTER = timedelta(days=3), daily_actions.py:42) is the ONE honest staleness detector in the codebase, and …
- **Návrh opravy:** In yahoo_cache._fetch_and_cache_data track a per-section flag: if refresh_market was requested and current_price came back None or the section raised, return False (and do not write market_data_updated). In _upsert_cache stop unconditionally resetting error_count/last_successful_fetch — only reset when a real price was …
- **Ověřovatel:** Claim survives; I could not refute it. Verified line by line.

yahoo_cache.py:337-401: market fetch sits in a nested try whose `except Exception as e: logger.warning(f"Failed to fetch market data for {ticker}: {e}")` swallows without setting any flag or re-raising. Control falls through to `_upsert_cache(data)` (:412), `_log_refresh(..., success=True)` (:415-425), `logger.info( …

**4. Silent FX fabrication with no live/fallback signal anywhere in the stack. `backend/app/services/currency.py:91` falls back to constants stamped "updated 2025-01-11" whenever the uncached per-call CNB …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** services/currency.py:43 `# Fallback rates (updated 2025-01-11)` then FALLBACK_RATES with "USD": 22.50, "CAD": 16.50, "HKD": 2.88, "JPY": 0.15. currency.py:91: `fallback = cls.FALLBACK_RATES.get(currency, cls.FALLBACK_RATES["USD"])` — the second argument means ANY currency not in the 12-item dict returns 22.50 CZK. I ran CurrencyService against live CNB today and diffed: USD live 20.620 vs fallback 22.50 = +9.1%; CAD live 15.005 vs 16.50 = +10.0%; HKD live 2.630 vs 2.88 = +9.5%; JPY live 0.1299 vs 0.15 = +15.5%. And `CurrencyService.get_rate_to_czk('ZZZ')` returns 22.5. The function signature is `-> float` — it can never say "unknown". The response models carry no live/fallback flag either (r …
- **Dopad na misi:** Breaks (2) how-much-to-buy/sell and the "never fake a number to fill a gap" rule, with real money. routes/daily_actions.py:136 injects this exact function as `fx_rate_to_czk` into generate_daily_actions, which uses it at daily_actions.py:145-147 to compute portfolio_value_czk and at :351/:383 to size every BUY against …
- **Návrh opravy:** Change get_rate_to_czk to `-> float | None` (or return a (rate, source, as_of) tuple). Cache the CNB daily.txt once per day keyed by its own date header. Drop the `cls.FALLBACK_RATES["USD"]` default entirely — an unknown currency must return None. Make daily_actions treat a None rate as a data-gap warning ("⚠️ CHYBÍ KU …
- **Ověřovatel:** Tried to refute on all five standard grounds and failed on each.

REAL PATHS (claim omitted the `backend/app/` prefix, line numbers exact): `c:\Users\reath\Projects\Akcion\backend\app\services\currency.py`, `...\backend\app\routes\currency.py`, `...\backend\app\routes\daily_actions.py`, `...\backend\app\services\daily_actions.py`.

1. NOT REFACTORED. currency.py:43 is verbatim …

**5. Massive/Polygon's `/v2/aggs/ticker/{ticker}/prev` returns the previous day's close and Finnhub silently substitutes `pc` (prev close) when the live `c` quote is missing; both are returned from `get_cu …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** market_data.py:120: `url = f"{MarketDataService.MASSIVE_API_BASE}/v2/aggs/ticker/{ticker}/prev"` — the /prev endpoint is by definition the previous day's aggregate; :128-130 `close_price = data["results"][0].get("c") ... return float(close_price)`. Live probe confirms: t=1787342400000 = 2026-08-21 20:00 UTC. This value is returned from `get_current_price` (market_data.py:257-260 `price = MarketDataService._get_price_from_massive(ticker); if price is not None: logger.info(f"{ticker}: ${price:.2f} (Massive API)"); return price`) and assigned verbatim at routes/stocks.py:122 `stock_response.current_price = current_price`. Separately, market_data.py:166-170: `current_price = data.get("c"); if cu …
- **Dopad na misi:** This is verbatim the failure mode the owner named: "a missing price becomes ... the previous close rather than an explicit unknown". It corrupts (2) how much / when: routes/stocks.py:115-119 feeds this number into calculate_price_position against green_line/red_line to decide whether a stock is in the BUY zone. A stock …
- **Návrh opravy:** Return a typed quote (price, as_of, source, is_delayed) rather than a bare float from _get_price_from_massive / _get_price_from_finnhub. Do not silently swap "pc" for "c" — if "c" is absent, return the prev close explicitly tagged as prev_close and let the UI render "včerejší zavírací cena". Propagate as_of to stock_re …
- **Ověřovatel:** Tried every listed refutation angle; all failed, and one backfired in the claim's favor.

1. NOT refactored — cited lines match verbatim. market_data.py:120 `url = f"{...}/v2/aggs/ticker/{ticker}/prev"`; :128-130 `close_price = data["results"][0].get("c") ... return float(close_price)`; :166-170 Finnhub `c` → silent `pc` fallback; :257-260 returns the Massive value; routes/stoc …

**6. Linear R/R approximation is live in three places with three mutually inconsistent band sets, feeding two different screens. (1) backend/app/services/gomes_deep_dd.py:476-492 (bands 10/30/50/70/90) run …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/services/gomes_deep_dd.py:477 — `position_pct = ((data.current_price - data.green_line) / price_range) * 100` then `:481-492` bands `if position_pct <= 10: DEEP_VALUE / <=30: BUY_ZONE / <=50: ACCUMULATE / <=70: FAIR_VALUE / <=90: SELL_ZONE`.
backend/app/routes/stocks.py:70-72 — a SECOND linear implementation with DIFFERENT bands: `range_size = red_line - green_line` / `position = current_price - green_line` / `position_pct = (position / range_size) * 100`, then `:80-91` `<=25 BUY_ZONE / <=50 ACCUMULATE / <=75 FAIR_VALUE / <=100 SELL_ZONE`.
Consumed LIVE at frontend/src/components/InvestmentTerminal.tsx:1948 (inside WatchlistDetailModal, mounted at :4630): `{stock.conviction_score …
- **Dopad na misi:** Breaks (5) mechanical/systematic and (4) stop emotional mistakes. The app actively invites him to buy three real Gomes picks that the canon says are fairly valued, and hides a sell on a fourth. Every error is biased toward buying. This is the exact gap the canon flags as "❌ MISSING/WRONG" in §9 row 0 — it was fixed in …
- **Návrh opravy:** Delete both linear blocks. Have gomes_deep_dd.py and routes/stocks.py call `RiskRewardCalculator.calculate_rr_score(price, green_line, red_line)` and derive `price_position_pct = (10 - score) * 10`. Unify the band thresholds in one place (the two copies already disagree: 10/30/50/70/90 vs 25/50/75/100). Return None (no …
- **Ověřovatel:** I tried each of the standard refutation angles and all of them failed — the finding holds.

1. "Line no longer says that" — REFUTED AS A DEFENSE. `C:\Users\reath\Projects\Akcion\backend\app\services\gomes_deep_dd.py` still contains verbatim at :476-492 `position_pct = ((data.current_price - data.green_line) / price_range) * 100` with bands `<=10 DEEP_VALUE / <=30 BUY_ZONE / <=5 …

**7. The hard position cap that produces the CZK buy amount is an unvalidated number written to the DB with no range check on any write path (LLM response at intelligence_gomes.py:958, PATCH at stocks.py:6 …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/routes/intelligence_gomes.py:958 — `stock.max_allocation_cap = data.get("max_allocation_cap", 10.0)` where `data` is the parsed LLM JSON response. No clamping, no range check, no comparison to the deterministic engine.
The LLM is explicitly asked for it: backend/app/core/prompts_ticker_analysis.py:101 `"max_allocation_cap": 8.0,  // Gomes Logic: ANCHOR=12%, HIGH_BETA=8%, BIOTECH=3%` and backend/app/core/prompts_universal_intelligence.py:117 `"max_allocation_cap": NUMBER (percentage)`.
The read side is unconditional and live: frontend/src/components/InvestmentTerminal.tsx:2631-2633 — `if (stock?.max_allocation_cap) { return stock.max_allocation_cap; }` is the FIRST branch of `calc …
- **Dopad na misi:** Breaks (5) mechanical/systematic, not vibes — and (2) how much to buy. A language model's free-text guess silently becomes the multiplier on his total portfolio value. If it emits 25.0, the app tells him to fund a single position to 25% of family capital while the canon caps a Primary at 10%. Correctness-is-the-feature …
- **Návrh opravy:** Never persist an LLM-supplied cap into the field the sizing math reads. Either (a) drop the key from both prompts and compute `stock.max_allocation_cap` from `GomesLogicEngine.calculate_max_allocation(...)` at write time (routes/gomes.py:1805 already does exactly this), or (b) keep the LLM value in a separate advisory …
- **Ověřovatel:** Attempted refutation on all five suggested axes; every one failed, and live DB evidence strengthened the finding.

(1) Cited line is current: intelligence_gomes.py:958 is verbatim `stock.max_allocation_cap = data.get("max_allocation_cap", 10.0)` with `data` = parsed LLM JSON. No clamping.

(2) No upstream guard anywhere in the chain. models/stock.py:302 is a bare `Column(Float, …

**8. EmailChannel._format_html crashes on every call — an invalid f-string format spec makes email delivery impossible even after credentials are fixed**  
*Oblast: Časování a automatika*

- **Důkaz:** `backend/app/services/notifications.py:185-207` — inside an f-string (`html = f"""` at line 185):
```
195:                <td style="padding: 8px;">${alert.entry_price:.2f if alert.entry_price else '—'}</td>
...
199:                <td style="padding: 8px; color: green;">${alert.target_price:.2f if alert.target_price else '—'}</td>
203:                <td style="padding: 8px; color: red;">${alert.stop_loss:.2f if alert.stop_loss else '—'}</td>
207:                <td style="padding: 8px;">{alert.kelly_size * 100:.1f if alert.kelly_size else '—'}%</td>
```
Everything after `:` in a replacement field is the FORMAT SPEC, not an expression. Executed:
```
$ python -c "... f'{alert.entry_price:.2f …
- **Dopad na misi:** Breaks mission point (4) and violates the non-negotiable "never fake numbers". This is a booby trap on the repair path for Finding 1: the owner fixes the env vars, sees `alerts_sent: 3`, believes alerting is live, stops checking manually — and receives nothing, forever. A reported-but-undelivered SELL on a RED alert is …
- **Návrh opravy:** Precompute the strings before the f-string: `entry = f"${alert.entry_price:.2f}" if alert.entry_price else "—"` (same for target/stop/kelly), then interpolate `{entry}`. Separately, make `check_and_send_alerts` honest: capture `results = await notification_service.send_alert(alert)` and only `alerts_sent.append(alert)` …
- **Ověřovatel:** Claim survives on every axis I attacked.

CODE STILL PRESENT: backend/app/services/notifications.py lines 195/199/203/207 are verbatim as cited, inside the f-string opened at line 185.

GUARDED-UPSTREAM DEFENSE FAILS (the strongest available refutation): I executed the real method with real non-None floats — EmailChannel('s',587,'u','p','f@x','t@x')._format_html(Alert(ticker='A …

### P1 — blokuje misi

**9. Both frontend-reachable analysis endpoints (POST /api/gomes/deep-dd and POST /api/gomes/update-stock/{ticker}) unconditionally raise AttributeError at gomes_deep_dd.py:518 (`price_at_analysis=data.cur …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/services/gomes_deep_dd.py:508 `self.db.commit()` … then :513-519 `score_history = ConvictionScoreHistory(ticker=data.ticker.upper(), stock_id=stock.id, conviction_score=data.conviction_score, thesis_status=result.thesis_drift or "STABLE", action_signal=data.action_signal, price_at_analysis=data.current_price, analysis_source=analysis_source,)`. `data` is a `DeepDueDiligenceResult`, and backend/app/schemas/gomes.py:321-378 defines NO `current_price` field. Proven by direct repro in the project venv: `DeepDueDiligenceResult(...).current_price` -> `AttributeError: 'DeepDueDiligenceResult' object has no attribute 'current_price'`. Both callers wrap this in a blanket handler: backend/ …
- **Dopad na misi:** Breaks (5) mechanical/systematic and (4) prevent emotional mistakes. The ONLY two analysis actions in the app always report failure to the user, yet the conviction_score, action_verdict, green_line, red_line and sentiment were already committed one statement earlier. He retries — the score moves again, invisibly, each …
- **Návrh opravy:** Add `current_price: Optional[float] = None` to DeepDueDiligenceResult (schemas/gomes.py) and set it in gomes_deep_dd.py:_parse_response from the Yahoo price already fetched at :103, not from the model's echoed JSON. Then move `self.db.commit()` to AFTER the score-history/alert block so the whole update is one transacti …
- **Ověřovatel:** I tried to break this claim on every listed axis and could not. The bug is real and current.

1) The line still says that. `c:\Users\reath\Projects\Akcion\backend\app\services\gomes_deep_dd.py:508` is `self.db.commit()` (preceded by `self.db.refresh(stock)`), and :512-519 builds `ConvictionScoreHistory(... price_at_analysis=data.current_price, ...)`. Last touch to that statemen …

**10. The Gomes Buy Guard (verify_gomes_compliance) is dead code from the UI's perspective: it is mounted only on /api/trading/validate-order and /api/trading/order, which the frontend never calls, and its …**  
*Oblast: Emoční brzdy*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:2270-2290 — the complete validation before money is committed:
```
  const handleSubmit = async () => {
    if (!ticker.trim() || !shares || !avgCost) {
      setError('Ticker, shares, and average cost are required');
      return;
    }
    if (!selectedPortfolioId) {
      setError('Please select a portfolio or import CSV first');
      return;
    }

    setLoading(true);
    setError(null);

    try {
      // Use the positions API to add position
      await apiClient.addPosition(selectedPortfolioId, {
```
The receiving endpoint, backend/app/routes/portfolio.py:202-212, is equally bare:
```
@router.post("/portfolios/{portfolio_id}/positions …
- **Dopad na misi:** Breaks mission points (4) and (5) outright. This is the exact button an emotional buyer presses at 22:00 after a Discord message. The app knows the market is RED, knows cash runway is 3 months, knows the ticker is in Wait Time — and says nothing. The Buy Guard the owner paid for in code never executes on the one path t …
- **Návrh opravy:** Make add_position call verify_gomes_compliance (or evaluate_buy_guard + evaluate_dual_source_buy) server-side and return 403/422, and have AddPositionModal call POST /api/trading/validate-order before submit, rendering the block. Server-side is mandatory — a frontend-only check is bypassable and the docstring at gomes_ …
- **Ověřovatel:** The code-level mechanics of the claim are accurate and I could not refute them. InvestmentTerminal.tsx:2270-2300 handleSubmit is verbatim as cited — two presence checks then apiClient.addPosition, no Gomes call. backend/app/routes/portfolio.py:202-280 add_position is as cited; its only validation is Pydantic Field(..., gt=0) on shares_count/avg_cost, and it never queries Stock, …

**11. The Path-2 Buy Guard is jammed shut: generate_verdict() (backend/app/services/gomes_intelligence.py:465-476) never passes cylinders_count to GomesGatekeeper.evaluate(), so it defaults to None (gomes_l …**  
*Oblast: Emoční brzdy*

- **Důkaz:** backend/app/services/gomes_intelligence.py:465-476 — the only production caller of GomesGatekeeper.evaluate():
```
        verdict = gatekeeper.evaluate(
            ticker=ticker,
            conviction_score=conviction_score,
            lifecycle_phase=lifecycle_phase,
            current_price=current_price,
            green_line=green_line,
            red_line=red_line,
            earnings_date=earnings_date,
            ml_prediction=ml_prediction,
            transcript_text=transcript_text,
            catalyst_info=catalyst_info
        )
```
`cylinders_count` is absent, so it defaults to None (gomes_logic.py:950 `cylinders_count: int | None = None`). RULE 7 then calls the guard …
- **Dopad na misi:** Every verdict shown through /api/gomes-intelligence/verdict/{ticker} is permanently HOLD, no matter how good the setup. It also poisons master_signal: backend/app/trading/master_signal.py:398-406 maps the verdict to a score, so a perfect stock scores `InvestmentVerdict.HOLD: 50.0` instead of `STRONG_BUY: 95.0`. Breaks …
- **Návrh opravy:** Pass `cylinders_count=lifecycle.cylinders_count if lifecycle else None` at gomes_intelligence.py:465, and add a regression test asserting the buyable fixture from tests/test_buy_guard.py:206 survives the service layer, not just the class.
- **Ověřovatel:** The finding survives every refutation angle I tested.

CODE VERIFIED (live file is backend/app/trading/gomes_logic.py — a stale duplicate exists at backend/app/core/gomes_logic.py, but gomes_intelligence.py:35 imports from app.trading.gomes_logic, and the claim's line numbers match the trading/ copy exactly):
- gomes_intelligence.py:465-476 — the evaluate() call is verbatim as …

**12. No usable trade ledger exists, making cooldown-after-loss and revenge-trading detection structurally impossible. A partial ledger table DOES exist — `InvestmentLog` / `investment_logs` (backend/app/mo …**  
*Oblast: Emoční brzdy*

- **Důkaz:** backend/app/models/portfolio.py declares only four tables: `class Portfolio(Base)` :47, `class Position(Base)` :118, `class MarketStatus(Base)` :250, `class InvestmentLog(Base)` :299. There is no Transaction/Trade model anywhere in backend/app/models/. A sale is a destructive in-place mutation — AssetDetailModal.tsx:97-98:
```
      const remainingShares = position.shares_count - sharesToTrim;
      await apiClient.updatePosition(position.id, { shares_count: remainingShares });
```
Grep for `realized` across backend/app returns only `unrealized_pl` computed properties (portfolio.py:217, :226). Grep for `cooldown|revenge|panic|impuls` across backend/app and frontend/src returns nothing but a …
- **Dopad na misi:** Breaks mission point (4) at the foundation. A cooldown needs 'when did he last realize a loss, and how big'; revenge detection needs 'how many trades in the last 48h and were they after a drawdown'. Neither datum exists or can be reconstructed — after a sale the app cannot even tell you the position ever existed. This …
- **Návrh opravy:** Add a Transaction model (ticker, side, shares, price, executed_at, realized_pl, portfolio_id); write a row on every add_position/update_position/delete_position; then implement the cooldown as a rule in daily_actions.py plus a hard check in add_position (e.g. refuse or require an explicit override within N days of a re …
- **Ověřovatel:** Verified every element and the finding holds; I also found evidence stronger than what was cited.

MODEL INVENTORY: backend/app/models/ has 25 Base subclasses across 8 files (the claim only surveyed portfolio.py, understating the surface). None is a transaction/trade record. Confirmed no `transactions`/`trades` table in models or migrations.

THE CLAIM UNDERSOLD ONE THING, WHIC …

**13. Confirmed and broader than stated: no confirm-with-reason step exists before any position write, the emotion_tag column and POST /portfolio/logs endpoint have zero callers in frontend/src, AND the one …**  
*Oblast: Emoční brzdy*

- **Důkaz:** The schema anticipated this. backend/app/models/portfolio.py:346-349:
```
    emotion_tag = Column(
        String(100),
        nullable=True,
        doc="Emotional context (e.g., 'Bál jsem se, ale koupil jsem dip')"
    )
```
And the endpoint exists: backend/app/routes/portfolio.py:1006-1013 `@router.post("/logs")` with `emotion_tag: str = None`. But grep for `portfolio/logs|emotion_tag|createInvestmentLog|InvestmentLog` across all of frontend/src returns ZERO hits — no client method, no call site. ConfirmDialog.tsx is imported only by PortfolioPage.tsx:6 and used only for the two deletes (PortfolioPage.tsx:228 'Are you sure you want to delete this position?' and :251 delete-all). No trad …
- **Dopad na misi:** Mission point (4) verbatim: 'I sometimes get carried away by emotion. The app should prevent that.' The single cheapest intervention — forcing him to type why he is overriding the guard before the trade commits — is half-built and unreachable. Friction at the moment of impulse is the whole mechanism; without it every w …
- **Návrh opravy:** When evaluate_buy_guard returns False for the ticker being added, replace the submit with a two-step dialog: show the failed gate verbatim, require a free-text reason (min length), write it as an InvestmentLog with emotion_tag before the position write, and surface those overrides in the weekly summary.
- **Ověřovatel:** Every cited fact verified exactly. backend/app/models/portfolio.py:346-349 contains emotion_tag as quoted. backend/app/routes/portfolio.py:1006-1013 defines POST /logs with emotion_tag param, constructing InvestmentLog. grep -rniE "portfolio/logs|emotion_tag|emotionTag|createInvestmentLog|InvestmentLog" over frontend/src returns ZERO hits; frontend/src/api/client.ts exposes onl …

**14. LIVE and unfixed: the Buy/Sell buttons in StockDetail's Trading tab are console.log no-ops (StockDetail.tsx:183-189, wired at :350-351, rendered enabled at TradingDeck.tsx:232-266). Worse than a dupli …**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/StockDetail.tsx:183-189 — `const handleBuyClick = () => {\n    console.log(`Buy clicked for ${stock.ticker}`);\n  };\n\n  const handleSellClick = () => {\n    console.log(`Sell clicked for ${stock.ticker}`);\n  };` — wired at StockDetail.tsx:350-351 `onBuyClick={handleBuyClick} onSellClick={handleSellClick}`; consumed at frontend/src/components/stock-detail/TradingDeck.tsx:232-234 `<button onClick={onBuyClick} disabled={!isTradeable || !isBuyAction}` and :252-254 `<button onClick={onSellClick} disabled={!isSellAction}`. The buttons render ENABLED and green/red (`bg-positive text-white ... cursor-pointer`, TradingDeck.tsx:239) whenever the verdict allows. StockDetail i …
- **Dopad na misi:** This is the exact regression project memory records as previously found and fixed (a Trim/Sell button that was a console.log). Mission point (2) 'advise HOW MUCH to sell and WHEN' and point (5) 'mechanical, not vibes' both die here: the app tells him to sell, he clicks 'Prodat GKPRF', the button lights up, nothing is r …
- **Návrh opravy:** Either delete both buttons and replace with the honest 'proveď u brokera, pak zaznamenej' note DailyActionWidget.tsx:90-94 already uses, or wire them to a real record-trade flow that POSTs a transaction. Never leave an enabled button whose handler is a log statement.
- **Ověřovatel:** Every cited element checks out verbatim, and each of the standard "claim is wrong" escape hatches fails.

1. Code still says that. C:\Users\reath\Projects\Akcion\frontend\src\components\StockDetail.tsx:183-189 is exactly `const handleBuyClick = () => { console.log(...) }` / `handleSellClick` — no side effect, no API call, no state change, no toast.
2. Wiring is real. StockDetai …

**15. LIVE: the "Trh" traffic-light badge in the stock modal is hardcoded GREEN because no caller passes StockDetail's `marketAlert` prop (default 'GREEN' at StockDetail.tsx:83; call sites InvestmentTermina …**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/StockDetail.tsx:83 — `marketAlert = 'GREEN'` (default prop). No caller passes it: InvestmentTerminal.tsx:4615-4625 renders `<StockDetail position={selectedPosition} onClose={...} onUpdate={...} />` with no marketAlert; PortfolioView.tsx:405 likewise. It flows to frontend/src/components/stock-detail/SafetyBadge.tsx:133 `marketAlert = 'GREEN',` and :147-153 renders `<SafetyBadge label="Trh" value={marketAlert} status={marketStatus} tooltip="Aktuální stav trhu podle Gomes Traffic Light" />` — a green 'safe' badge reading GREEN, always. It also flows to stock-detail/TradingDeck.tsx:56 `if (props.marketAlert === 'RED') { blockers.push({reason: 'Trh je v režimu RED - Cash i …
- **Dopad na misi:** Breaks point (4) 'stop him from making emotional mistakes' and point (5). In a RED market DailyActionWidget correctly shows '🔴 MARKET ALERT: RED — hotovost je král' (DailyActionWidget.tsx:38), but opening any stock shows 'Trh: GREEN', 'Trading povoleno' and 'Všechny bezpečnostní kontroly prošly' (TradingDeck.tsx:143,18 …
- **Návrh opravy:** Fetch market status once in InvestmentTerminal (apiClient.getMarketStatus() already exists, client.ts:345) and pass it down. Remove the `= 'GREEN'` defaults in StockDetail.tsx:83 and SafetyBadge.tsx:133; make the prop required and render 'NEZNÁMÝ' (status 'unknown') when it is null. Widen the type to include 'ORANGE' — …
- **Ověřovatel:** Every cited line is current and every escape hatch I checked is closed.

VERIFIED AS CLAIMED:
- C:\Users\reath\Projects\Akcion\frontend\src\components\StockDetail.tsx:67,83 — `marketAlert?: 'RED'|'YELLOW'|'GREEN'` with `marketAlert = 'GREEN'` default. It is forwarded to SafetyGaugeRow (line 258) and TradingDeck (line 341).
- Only two call sites exist (`grep -rn "<StockDetail"`) …

**16. LIVE: the headline "Unrealized P/L" percentage in InvestmentTerminal.tsx:3574-3576 divides two differently-filtered aggregates — total market value (which includes unknown-cost positions) over total c …**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:3566-3577 — `const totals = portfolios.reduce((acc, p) => ({ costBasis: acc.costBasis + (p.total_cost_basis || 0), marketValue: acc.marketValue + (p.total_market_value || 0), ... }));` then `const plPercent = totals.costBasis > 0 ? ((totals.marketValue / totals.costBasis) - 1) * 100 : 0;` rendered at :3597-3599 as `({plPercent >= 0 ? '+' : ''}{plPercent.toFixed(2)}%)` under the label 'Unrealized P/L'. The backend deliberately puts unknown-cost positions in market value but NOT in cost basis: backend/app/routes/portfolio.py:134-141 `if cost_basis is not None ...: total_cost_basis_czk += cost_basis * rate` / `if not math.isnan(market_value)...: to …
- **Dopad na misi:** Concrete failure: one Trading-212 position with known cost 100k CZK now worth 110k, plus one Degiro position with unknown cost now worth 500k → tile reads '+510.00%' instead of the true '+10.00%'. Breaks the non-negotiable 'never fake numbers filling gaps' and point (3) 'hit at least 15% annually' — he cannot tell whet …
- **Návrh opravy:** Sum `p.total_unrealized_pl_percent` weighted by `p.total_cost_basis`, or simply use the backend's per-portfolio value. Add a badge on the tile when `positions.some(p => p.avg_cost == null)`: 'P/L počítáno jen z N pozic se známou nákupní cenou'.
- **Ověřovatel:** The finding survives every refutation check.

1. NOT refactored. InvestmentTerminal.tsx:3566-3577 reads verbatim as cited: totals.costBasis sums p.total_cost_basis, totals.marketValue sums p.total_market_value, and plPercent = totals.costBasis > 0 ? ((totals.marketValue / totals.costBasis) - 1) * 100 : 0. Rendered at :3594-3598 inside the "Unrealized P/L" tile, where the correc …

**17. yfinance 0.2.66 raises KeyError: 'currentTradingPeriod' on two of the owner's actual holdings -- IMP.V and KUYA.V -- because market_data.py:71,77 hardcodes the wrong exchange suffixes (correct: IMP.TO …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** market_data.py:75-83 hardcodes `"GATEKEEPER SYSTEMS": "GSI.V"`, `"INTERMAP TECHNOLOGIES": "IMP.V"`, `"KUYA SILVER": "KUYA.V"`, `"NETDRAGON WEBSOFT": "0777.HK"`. Tested each against the installed yfinance 0.2.66: GSI.V -> 1.77 CAD OK, 0777.HK -> 7.57 HKD OK, but IMP.V, KUYA.V and QIPT.TO all raise `KeyError: 'currentTradingPeriod'`, and for those three `stock.info` returns an empty dict (longName=None, currentPrice=None, regularMarketPrice=None). The correct suffixes DO price cleanly on the same library in the same session: IMP.TO -> 1.03 CAD, KUYA.CN -> 0.73 CAD. The raise comes from site-packages/yfinance/scrapers/quote.py:104 `def get(self, key, default=None): return self[key]` — FastInfo …
- **Dopad na misi:** Breaks (2) for real positions: three holdings can never get a price from the primary path, so they hit finding 4 (market_value 0.00) and finding 1 (silent success). And because a wrong ticker and a network outage produce the identical KeyError swallowed at yahoo_cache.py:364, the owner can never tell "Yahoo is down" fr …
- **Návrh opravy:** Two separate fixes. (a) Never call FastInfo.get bare — wrap yahoo_cache.py:347-354 per-field or use `stock.info.get('regularMarketPrice')` as the cross-check, and treat an all-None info dict as an explicit failure. (b) Correct market_data.py:75-83 to IMP.TO and KUYA.CN (both verified priceable today), and add a startup …
- **Ověřovatel:** Could not refute. The cited code exists (backend/app/services/market_data.py:59-91; entries at 69/71/77/80, not "75-83"), and I reproduced the failure live against the installed yfinance 0.2.66: GSI.V -> 1.77 CAD OK, 0777.HK -> 7.57 HKD OK, IMP.V/KUYA.V/QIPT/QIPT.TO -> KeyError: 'currentTradingPeriod'; IMP.TO resolves to "Intermap Technologies Corporation" (TOR, 1.03 CAD) and K …

**18. Nothing refreshes market data in the background. The only background task started (app/main.py:101 `await start_scheduler()`) is the alert scheduler, and it is inert three times over: (1) its gate `is …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** app/main.py:101-104 starts only the alert scheduler (`await start_scheduler()`). grep for refresh_portfolio_prices, YahooFinanceCache, get_current_price and bulk_refresh across services/alert_scheduler.py, services/notifications.py and services/notification_service.py returns ZERO hits — the scheduler never fetches a price. Its gate is services/alert_scheduler.py:47-52: `def is_market_hours() -> bool: now = datetime.now().time(); market_open = time(MARKET_OPEN_HOUR, 0); market_close = time(MARKET_CLOSE_HOUR, 0); return market_open <= now <= market_close` with MARKET_OPEN_HOUR = 9 and MARKET_CLOSE_HOUR = 16 (:39-40) — naive `datetime.now()`, no timezone, while the app's own correct NYSE helpe …
- **Dopad na misi:** Breaks the "survive weeks of absence" non-negotiable head-on. Data only moves when he manually opens the app and presses refresh — precisely what he cannot do during an MS relapse. On a Czech host (the app's currency and UI are CZK/Czech) 9:00-16:00 local is 03:00-10:00 ET, so the alert scheduler runs almost entirely w …
- **Návrh opravy:** Point alert_scheduler.is_market_hours at the existing core.market_hours.is_market_open() instead of naive datetime.now() — the correct, timezone-aware, holiday-aware implementation is already in the repo. Then add a scheduled job that calls YahooFinanceCache.bulk_refresh over the portfolio + watchlist once after the US …
- **Ověřovatel:** I tried hard to refute this and could not — every cited fact is verbatim accurate, and I found two additional defects that make it worse.

1. main.py startup is exactly as cited. C:\Users\reath\Projects\Akcion\backend\app\main.py lines 88-105: the single `@app.on_event("startup")` handler does `initialize_database(...)` then `await start_scheduler()`. That is the only backgroun …

**19. All Gemini calls target gemini-2.0-flash (backend/app/core/constants.py:143 plus three hardcoded sites), which returns HTTP 404 "no longer available" when called with the app's own API key — verified …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** backend/app/core/constants.py:143 `model_name: str = "gemini-2.0-flash"` — resolved into `GEMINI_MODEL_NAME` at backend/app/core/prompts.py:269. Hardcoded at three more sites: backend/app/routes/intelligence_gomes.py:829 `model = genai.GenerativeModel('gemini-2.0-flash')`, backend/app/services/gomes_deep_dd.py:73 `self.model = genai.GenerativeModel('gemini-2.0-flash')`, backend/app/services/knowledge_synthesis.py:121 `self.model = genai.GenerativeModel('gemini-2.0-flash')`. Google's deprecation page and independent coverage both state gemini-2.0-flash reached its shutdown date 2026-06-01 and "Any call to these endpoints after June 1 failed." Meanwhile backend/app/routes/analysis.py:522-525 r …
- **Dopad na misi:** Kills mission point (1) outright — pressing "Analyze" cannot reach any model, so no news is read or summarized. Kills the AI half of point (2). Worst for point (5)/trust: the ONLY diagnostic the owner has, /api/analyze/health, returns "healthy" and names a model the app does not use and that is itself retired, so the g …
- **Návrh opravy:** Migrate the 5 call sites from the EOL google-generativeai SDK to google-genai and pick a live model id (verify against ai.google.dev/gemini-api/docs/deprecations at the time of the fix; gemini-2.5-flash itself shuts down 2026-10-16). Centralise the id in constants.py so no literal is hardcoded in services/routes. Rewri …
- **Ověřovatel:** Attempted refutation on all five common failure modes; none applied.

1. "Line no longer says that" — FALSE. All cited lines are verbatim accurate today: backend/app/core/constants.py:143 (model_name: str = "gemini-2.0-flash"), backend/app/core/prompts.py:269 (GEMINI_MODEL_NAME = GEMINI_CONFIG.model_name), backend/app/routes/intelligence_gomes.py:829, backend/app/services/gomes …

**20. Zero automated coverage of the price/market-data layer and only 1 of 133 API routes tested, while yfinance is pinned exactly at 0.2.66 (latest 1.6.0) with no dependabot. Three test files — test_yahoo_ …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** `pytest tests/test_yahoo_cache.py -q` -> `19 skipped` (19 collected, 19 skipped: 15 by the module-level legacy pytestmark, 4 integration-gated). Nothing else tests the price path. Enumerating app.routes and scanning all 45 frontend .ts/.tsx files: 133 distinct /api routes registered; active tests touch exactly one, `/api/trading/daily-actions` (tests/test_daily_actions_endpoint.py:412 and :425). backend/requirements.txt pins `yfinance==0.2.66` — an exact pin on a release dated 2025-09-17; the current release is 1.6.0 (2026-08-13). yfinance is an unofficial scraper of Yahoo endpoints, so a hard `==` pin cannot self-heal when Yahoo changes response shapes. On failure backend/app/services/yahoo …
- **Dopad na misi:** Prices drive every buy/sell size and every P/L number, i.e. mission point (2) and the "correctness is the feature" non-negotiable. A silent yfinance breakage would flow through as stale-but-normal-looking prices with nothing in CI or the test suite to catch it. The 148-passing green suite creates false confidence: it p …
- **Návrh opravy:** Un-skip and repair tests/test_yahoo_cache.py first — it is the layer closest to the money. Add a recorded-fixture test that asserts the yfinance response parses into the expected fields, so a Yahoo shape change fails CI instead of silently returning stale cache. Have get_stock_data return an explicit `is_stale`/`fetche …
- **Ověřovatel:** Every cited fact verified exactly, and the standard refutation levers all failed.

CONFIRMED VERBATIM:
- `pytest backend/tests/test_yahoo_cache.py -q` -> `19 skipped`. Module-level `pytestmark = pytest.mark.skip(...)` at backend/tests/test_yahoo_cache.py:13.
- Enumerating `app.routes` yields exactly 133 distinct /api paths. Number is precise.
- Only active endpoint assertions a …

**21. The `code-quality` job cannot fail on findings: both of its check steps (ci.yml:135 ruff check, :140 ruff format --check) pipe a non-zero exit into `|| echo`, as does the frontend Lint step (:105). Al …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** .github/workflows/ci.yml:135 `ruff check app/ --select E,F,W || echo "Linting warnings detected"`, :140 `ruff format --check app/ || echo "Formatting issues detected"`, and :105 `npm run lint || echo "Lint warnings detected"` — every one swallows a non-zero exit into a successful echo, so the entire code-quality job reports green unconditionally. Line 100 is `npm run type-check || npx tsc --noEmit`, but frontend/package.json declares only `dev`, `build`, `lint`, `preview` — there is no `type-check` script, so npm always exits non-zero and the real check runs solely via the `||` fallback.
- **Dopad na misi:** Against point (5) and "correctness is the feature": a green CI badge is the owner's proxy for "safe to trust after a month away", and three of its checks are decorative. Combined with the 54 skipped tests, a fully green pipeline is compatible with a broken price layer, an unreachable model, and unformatted/lint-failing …
- **Návrh opravy:** Drop the `|| echo` from ci.yml:105, :135 and :140 so lint and format failures actually fail the job (loosen the ruff rule selection first if the current tree is noisy). Add `"type-check": "tsc --noEmit"` to frontend/package.json and reduce ci.yml:100 to `npm run type-check`. Add a CI step asserting the skip count does …
- **Ověřovatel:** I tried every listed refutation path and none applied. The cited lines are current (ci.yml last touched at b925c01/c21462b, and the text matches exactly), there is only one workflow file, there is no .pre-commit-config.yaml, and no other job re-runs these checks.

VERIFIED LINE-BY-LINE (C:\Users\reath\Projects\Akcion\.github\workflows\ci.yml):
- :105 `npm run lint || echo "Lint …

**22. The only share-count BUY path hardcodes the USD FX rate and a "USD" label; the Stock model has no currency column, so `AnalysisInput` cannot carry one. Confirmed at backend/app/services/daily_actions. …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/services/daily_actions.py:386-388 — `rate = fx_rate_to_czk("USD")` / `price_czk = price * rate` / `quantity = math.floor(budget_czk / price_czk) if price_czk > 0 else 0`, and :409 `currency="USD"` is hardcoded into the ActionItem.
The currency is not merely unused — it does not exist: `AnalysisInput` (backend/app/services/daily_actions.py:69-79) has no currency field, backend/app/routes/daily_actions.py:101-113 cannot supply one, and `grep -n "currency" backend/app/models/stock.py` returns ZERO hits (the Stock table has 60+ columns, none of them currency). Contrast backend/app/models/portfolio.py:167 where `Position.currency` DOES exist and is correctly used for sells at daily_ac …
- **Dopad na misi:** Breaks (2) advise HOW MUCH to buy. The single reachable share-count recommendation in the app is arithmetically wrong for every non-USD ticker, including a stock on Gomes's official list. Today it under-buys (safe direction), but the sign of the error flips with any currency stronger than the fallback USD rate — and th …
- **Návrh opravy:** Add a `currency` column to Stock (default 'USD'), thread it through `AnalysisInput` and `load_daily_action_inputs`, then use `fx_rate_to_czk(analysis.currency)` at daily_actions.py:386 and `currency=analysis.currency` at :409. Until the column exists, refuse to emit a BUY for any ticker whose currency is unknown rather …
- **Ověřovatel:** I tried every listed refutation route and none of them holds. The finding survives.

Line-by-line verification:
- C:\Users\reath\Projects\Akcion\backend\app\services\daily_actions.py:386-388 still reads exactly `rate = fx_rate_to_czk("USD")` / `price_czk = price * rate` / `quantity = math.floor(budget_czk / price_czk) if price_czk > 0 else 0`. Not refactored.
- Same file :409 s …

**23. The alert scheduler is started at app startup (main.py:103) and runs every 30 minutes during 9:00-16:00 local time, but its notification factory can never build a channel: NotificationService.from_env …**  
*Oblast: Časování a automatika*

- **Důkaz:** Three independent breaks, all proven by executing the real code.

(1) Env-var names the scheduler needs do not exist anywhere. `backend/app/services/notifications.py:291-299`:
```
291:        from_email = os.getenv('SMTP_FROM_EMAIL')
292:        to_email = os.getenv('SMTP_TO_EMAIL')
...
294:        if all([smtp_server, smtp_user, smtp_pass, from_email, to_email]):
295:            service.add_channel(EmailChannel(
```
`SMTP_FROM_EMAIL` / `SMTP_TO_EMAIL` appear in NO .env file and in NO field of `backend/app/config/settings.py`. `backend/.env` defines `EMAIL_RECIPIENT` instead — a name this module never reads.

(2) Even the vars that ARE set are invisible to this code. `backend/app/config/sett …
- **Dopad na misi:** Breaks mission points (1), (2) and (4) at the same time. This is the exact scenario project memory calls unacceptable: a silent no-op dressed as normal operation. If the market alert flips RED or a thesis breaks during an MS relapse, the app has already computed `LIQUIDATE_HEAVY` / `SELL_WAIT_TIME` actions (`services/d …
- **Návrh opravy:** Delete `services/notifications.py`'s `from_env()` os.getenv path entirely and build channels from `get_settings()` — the same Settings object the rest of the app already uses and that already has `EMAIL_RECIPIENT`, `SMTP_*`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` populated. Add `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` …
- **Ověřovatel:** Verified by executing the real code, not just reading it.

CONFIRMED: (1) notifications.py:291-292 reads SMTP_FROM_EMAIL/SMTP_TO_EMAIL; neither name exists in backend/.env or settings.py (which uses EMAIL_RECIPIENT). (2) settings.py:126-130 uses pydantic-settings SettingsConfigDict(env_file=".env"), which populates the Settings object and not os.environ; the only load_dotenv in …

**24. The core claim stands as written. Only the secondary hazard needs correcting: backend/start_background.ps1:7 kills whatever holds port 8000 while run_server.py:26 binds 8002, so the kill is a no-op — …**  
*Oblast: Časování a automatika*

- **Důkaz:** The scheduler is an in-process asyncio task, not a service: `backend/app/services/alert_scheduler.py:122` `_scheduler_task = asyncio.create_task(scheduler_loop())`, started from `backend/app/main.py:103` and killed at `main.py:109-116` `@app.on_event("shutdown")` → `await stop_scheduler()`. Its lifetime is exactly the uvicorn process's lifetime.

That process is a hand-started dev server on the owner's own Windows PC:
- `backend/run_server.py:22-28`: `uvicorn.run("app.main:app", host="0.0.0.0", port=8002, reload=True, ...)`
- `start.ps1:10`: `Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd '$backendPath'; python run_server.py"`
- `frontend/.env`: `VITE_API_URL=http://localh …
- **Dopad na misi:** Breaks mission points (1) and (4) unconditionally, and makes Finding 1 unfixable in place. Even with channels and the formatter repaired, the loop only runs while he has personally started the server — i.e. exactly when he is well enough to open the app and does not need to be told. During the absence the whole feature …
- **Návrh opravy:** Move the recurring check off the owner's desktop. Cheapest honest option that needs no new infra: a `schedule:` cron in `.github/workflows/` (or any always-on host) that calls the alerting job against the same Postgres `DATABASE_URL` — `alert_scheduler.py:148-149` already supports standalone execution (`if __name__ == …
- **Ověřovatel:** Every cited line is verbatim accurate and I could not refute the core claim on any angle.

Verified: alert_scheduler.py:122 is exactly `_scheduler_task = asyncio.create_task(scheduler_loop())`; main.py:103 starts it in `@app.on_event("startup")` and main.py:109-116 cancels it on shutdown, so its lifetime is precisely the uvicorn process's. run_server.py:22-28 hardcodes `host="0 …

**25. Two `NotificationService` classes exist. `backend/app/services/notification_service.py` reads the pydantic `Settings` object (which does load `backend/.env`) and is correctly configured — `email_enabl …**  
*Oblast: Časování a automatika*

- **Důkaz:** There are two unrelated `NotificationService` classes.

Stack A — `backend/app/services/notification_service.py:39-59` — reads the SAME Settings object the app already loads:
```
44:        self.email_recipient = getattr(self.settings, 'EMAIL_RECIPIENT', None)
47:        self.smtp_username = getattr(self.settings, 'SMTP_USERNAME', None)
48:        self.smtp_password = getattr(self.settings, 'SMTP_PASSWORD', None)
50:        self.email_enabled = bool(
51:            self.email_recipient and self.smtp_username and self.smtp_password
52:        )
```
Executed against the real `backend/.env`: `=> notification_service.NotificationService email_enabled would be: True`. Its `_send_email` (`notifica …
- **Dopad na misi:** Breaks mission point (5) and is the root cause that let Findings 1 and 2 survive: the owner can hit `/api/invest/notify/test`, receive a real email, and reasonably conclude alerting works — while the 30-minute background loop that is supposed to protect him during an absence is bound to the other, dead stack. Two sourc …
- **Návrh opravy:** Pick `services/notification_service.py` (the Settings-based one that demonstrably works), port `TelegramChannel` onto it, and delete `services/notifications.py`'s `NotificationService`/`from_env`/`EmailChannel`, repointing `alert_scheduler.py:23` and `routes/notifications.py:17-21` at the survivor. Collapse `/api/notif …
- **Ověřovatel:** I tried every listed refutation path and none of them held.

1. "The cited lines no longer say that." They do. `backend/app/services/notification_service.py:39-59` is verbatim the `__init__` quoted, including `email_enabled = bool(email_recipient and smtp_username and smtp_password)` at lines 50-54. `backend/app/services/notifications.py:280-301` is the `from_env()` classmethod …

### P2 — tření

**26. Deep DD's Czech Part 1 executive summary is generated, paid for, and discarded: the API returns `analysis_text` (gomes.py:1131) but InvestmentTerminal.tsx:3214 never assigns it, and update_stock_from_ …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** Backend produces it: backend/app/routes/gomes.py:1131-1139 returns `{"analysis_text": result.analysis_text, "data": …, "analyzed_at": result.analyzed_at.isoformat(), …}`, built from the prompt at core/prompts_enterprise_v2.py:486-505 (`=== ČÁST 1: EXECUTIVE SUMMARY (Pro uživatele - česky) ===` with VERDIKT / SCORE / ONE-LINER / TOP 3 DŮVODY PRO / TOP 3 RIZIKA). Frontend types it: frontend/src/types/index.ts:533-535 `export interface DeepDDResponse { analysis_text: string;`. Frontend then drops it: frontend/src/components/InvestmentTerminal.tsx:3214 `await apiClient.runDeepDD(transcript, ticker);` — no assignment, no render. `grep -rn "analysis_text" frontend/src` matches only the type declar …
- **Dopad na misi:** Kills (1) "summarizes it for him" and (3) motivation. He pastes a transcript, waits, gets "Analysis failed. Please try again.", and the only visible outcome anywhere in the app is that a 0-10 number moved. The reasoning — why the score moved, the three reasons for, the three risks — is generated and discarded. There is …
- **Návrh opravy:** In gomes_deep_dd.py:update_stock_from_analysis, write `stock.raw_notes = result.analysis_text` (the column exists, models/stock.py:156) alongside a written analysed-at timestamp. In InvestmentTerminal.tsx:3199-3221, capture the DeepDDResponse and show `analysis_text` in a result panel before closing the modal, instead …
- **Ověřovatel:** I could not refute the mechanical core. All four cited facts check out, and I looked specifically for the standard escape hatches:

1. Backend returns it — CONFIRMED. c:\Users\reath\Projects\Akcion\backend\app\routes\gomes.py:1131 returns `"analysis_text": result.analysis_text`, built in c:\Users\reath\Projects\Akcion\backend\app\services\gomes_deep_dd.py:355-365 (`analysis_tex …

**27. gomes_deep_dd.py's update_stock_from_analysis writes none of cash_runway_months, inflection_status, thesis_narrative or next_catalyst, and its price_zone block (:473-492) is unreachable because DeepDu …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/services/gomes_deep_dd.py:457-506 is the complete set of writes; `grep -n "stock\.\(cash_runway_months\|thesis_narrative\|next_catalyst\|inflection_status\|raw_notes\|insider_activity\|max_allocation_cap\)" backend/app/services/gomes_deep_dd.py` returns NOTHING (exit 1) — even though DeepDueDiligenceResult carries `cash_runway_months` (schemas/gomes.py:352) and the model is asked for it. Separately, :473 `if hasattr(data, 'current_price') and data.current_price and data.green_line and data.red_line:` guards :474-492 (the whole price_position_pct / DEEP_VALUE / BUY_ZONE / ACCUMULATE / FAIR_VALUE / SELL_ZONE / OVERVALUED assignment) — and `hasattr` is provably False, verified in th …
- **Dopad na misi:** Breaks (4) prevent emotional mistakes. StockDetail.tsx:250-255 feeds `cashRunwayMonths={stock.cash_runway_months ?? null}` and `inflectionStatus={stock.inflection_status ?? null}` into SafetyGaugeRow and TradingDeck, where TradingDeck.tsx:62-76 turns them into the two hardest safety gates — `Cash Runway pouze X měsíců …
- **Návrh opravy:** After adding `current_price` to the schema (see the AttributeError finding), extend update_stock_from_analysis to write cash_runway_months, inflection_point_status -> inflection_status, thesis_narrative and next_catalyst, and let the price_zone block run. Where the model returns nothing for cash_runway_months, write nu …
- **Ověřovatel:** The two literal code claims are verifiably true, so I cannot refute the finding outright — but the mission-impact argument that justifies P1 collapses on three checks.

VERIFIED TRUE:
- backend/app/services/gomes_deep_dd.py:457-506 is the complete write set in update_stock_from_analysis; cash_runway_months, inflection_status, thesis_narrative, next_catalyst are never assigned t …

**28. A working, API-keyed Polygon.io news fetcher exists in the backend and no part of the UI can reach it**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/services/news_monitor.py:126-171 `async def fetch_news_polygon(self, ticker: str, limit: int = 10, days_back: int = 7)` -> `url = (f"https://api.polygon.io/v2/reference/news?ticker={ticker}&published_utc.gte={published_after}&limit={limit}&sort=published_utc&order=desc&apiKey={self.polygon_api_key}")` with real classification logic at :173-230 (CATALYST_KEYWORDS / RISK_KEYWORDS / NOISE_KEYWORDS at :95-116). Exposed at backend/app/routes/investment.py:129 `@router.get("/news/{ticker}")` under prefix `/api/invest` (:18), mounted at backend/app/main.py:134 `app.include_router(investment.router)`. The key is configured: backend/.env contains `MASSIVE_API_KEY=BarJ…` (settings.py:46-50 …
- **Dopad na misi:** This is requirement (1) sitting fully built, paid for, and disconnected — the shortest path from today's app to "press Analyze and read current news" is a client method and a button, not new infrastructure. Meanwhile he is paying for a Polygon key that the product never uses, and the only "news" the app understands is …
- **Návrh opravy:** Add `getStockNews(ticker)` to api/client.ts hitting `/api/invest/news/${ticker}`, render the classified articles (with their published_utc dates) in StockDetail's Analýza tab, and add an "Analyzovat aktuální zprávy" button that pipes those article bodies into POST /api/gomes/update-stock/{ticker} as the transcript with …
- **Ověřovatel:** The claim survives every refutation angle, confirmed by execution rather than reading alone.

CODE IS THERE AS CITED: backend/app/services/news_monitor.py:126-171 contains fetch_news_polygon with the exact Polygon URL; CATALYST/RISK/NOISE keyword lists at :99-116; classify_news at :173-230; news_item_to_dict at :423. backend/app/routes/investment.py:18 sets prefix /api/invest, …

**29. Four analyze-related components are confirmed unreachable dead code — AssetDetailModal, AnalyticsDashboard (whose component is actually named GomesDashboard), QuickNoteInput, and Sidebar — plus the se …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** frontend/src/App.tsx renders only `<InvestmentTerminal />`; InvestmentTerminal.tsx:23-26 imports only StockDetail, NotificationBell, DailyActionWidget, ClearPortfolioButton. Grepping frontend/src for imports of AssetDetailModal, AnalyticsDashboard, QuickNoteInput and Sidebar returns only their own definitions (AssetDetailModal.tsx:77, AnalyticsDashboard.tsx:20, QuickNoteInput.tsx:33) — nothing imports them. AssetDetailModal.tsx:977 hardcodes the host: `const response = await fetch('http://127.0.0.1:8002/api/intelligence/analyze-ticker', {` (bypassing api/client.ts and its VITE_API_URL), then :1015 `window.location.reload();` and :1013 `alert(alertMsg);`. InvestmentTerminal.tsx itself flags o …
- **Dopad na misi:** Mostly a maintenance hazard rather than an active money risk, but it distorts every attempt to reason about the app: the richest analyze UI in the repo (AssetDetailModal's source-type detection, warning messages, Universal Intelligence mode via /api/intelligence/analyze-ticker) is unreachable, so an auditor or future c …
- **Návrh opravy:** Either mount AssetDetailModal (after replacing the hardcoded 127.0.0.1:8002 with apiClient and the alert()/reload with in-modal state) or delete it along with AnalyticsDashboard, QuickNoteInput, Sidebar and StockDetailModal. For TradingDeck, make the buttons either open the manual-execution checklist or be visibly disa …
- **Ověřovatel:** I tried to break this finding on all four of the usual escape hatches and it survived every one.

MOUNT PATH — `c:\Users\reath\Projects\Akcion\frontend\src\main.tsx` renders only `<App/>`; `App.tsx` renders only `<ToastProvider><InvestmentTerminal/><ToastContainer/></ToastProvider>`. `InvestmentTerminal.tsx:23-26` imports exactly StockDetail, NotificationBell, DailyActionWidget …

**30. `app.services.gomes_gatekeeper` does not exist, breaking two verdict-refresh paths. In `backend/app/routes/analysis.py:68`, the failed import is swallowed by `except ImportError` and logged at INFO, s …**  
*Oblast: Emoční brzdy*

- **Důkaz:** `find . -name "gomes_gatekeeper*"` returns nothing; backend/app/services/ contains no such file. Two callers import it. backend/app/routes/analysis.py:68-69 `from ..services.gomes_gatekeeper import GomesGatekeeper` / `gatekeeper = GomesGatekeeper(db)`, swallowed at analysis.py:98-99:
```
        except ImportError:
            logger.info("Gatekeeper not available, skipping verdict refresh")
```
And backend/app/routes/gomes.py:1585 `from app.services.gomes_gatekeeper import GomesGatekeeper` inside `@router.post("/refresh-all-verdicts")` (gomes.py:1571), where the only handler is `except Exception as e: raise HTTPException(status_code=500, ...)` at gomes.py:1636.
- **Dopad na misi:** After importing a new transcript, verdicts are never recomputed — the app keeps showing yesterday's (already permanently-HOLD, per Finding 3) verdict with no staleness marker. Breaks the 'survive weeks of absence with honest stale-data labelling' non-negotiable: the failure is logged at INFO level and shown to the user …
- **Návrh opravy:** Either implement services/gomes_gatekeeper.py with evaluate_ticker(), or repoint both call sites at GomesIntelligenceService.generate_verdict (after fixing Finding 3). Do not leave the ImportError swallow — a guardrail engine that is absent must surface, not whisper.
- **Ověřovatel:** The core technical claim survives every refutation attempt I made.

VERIFIED TRUE:
1. `backend/app/services/gomes_gatekeeper.py` does not exist. `ls backend/app/services/` shows 20 files, none named gatekeeper; a repo-wide find for `*gatekeeper*` returns only two frontend files (`frontend/src/components/GatekeeperShield.tsx`, `frontend/src/hooks/useGatekeeperStatus.tsx`). The r …

**31. The /api/invest/* Investment Intelligence surface (Polygon news fetch + classification + entry-zone alerts) is fully built server-side and has zero callers anywhere in frontend/src — no UI can reach i …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** The backend is wired: app/main.py:134 `app.include_router(investment.router)  # Investment Intelligence`, exposing GET /api/invest/news/{ticker} (routes/investment.py:159 `articles = await news_service.fetch_news_polygon(ticker.upper(), limit=limit)`), GET /api/invest/news (:182), GET /api/invest/alerts (:105) and GET /api/invest/analyze/{ticker} (:41). trading/data_fetcher.py is exposed at POST /api/trading/sync/data (routes/trading.py:187, :206-209 `data_fetcher = DataFetcher(db, settings); await data_fetcher.fetch_multiple_tickers(...)`). Both live-probe healthy. But `grep -rn "api/invest|api/yahoo|api/trading/sync" frontend/src` returns ZERO hits, and the exhaustive endpoint list from fr …
- **Dopad na misi:** Breaks (1) and (4). Real news classification (news_monitor.classify_news, :175-267) and entry-zone alerts (check_entry_zones, :269-327) are built, working, and invisible. The owner cannot press anything that reaches them, so "the app reads the news for me" is false at the UI layer even though the data path is alive. It …
- **Návrh opravy:** Cheapest high-value win in this whole audit: add a client.ts method for GET /api/invest/news/{ticker} and render it in AssetDetailModal next to the Analyze button. No new vendor, no new key, no new dependency — the endpoint and the API quota already exist and were verified returning real articles today.
- **Ověřovatel:** I tried hard to break this one and the central fact holds, but two of its three limbs are overstated.

WHAT SURVIVES (verified):
- `C:\Users\reath\Projects\Akcion\backend\app\main.py:134` still reads `app.include_router(investment.router)  # Investment Intelligence`, and `:137` `app.include_router(yahoo_finance.router)`. Not refactored away.
- `backend\app\routes\investment.py: …

**32. analyze_ticker_from_transcript (backend/app/routes/intelligence_gomes.py:802) passes `float(stock.current_price or 0)` into the Gemini prompt, which renders as "Price $0.0" whenever Stock.current_pric …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** routes/intelligence_gomes.py:802, inside analyze_ticker_from_transcript — the handler behind the frontend's Analyze button: `current_price = float(stock.current_price or 0)`. Two lines later `shares_count = float(position.shares_count if position else 0)` (:803). That value is then interpolated into the Gemini prompt at :818 `prompt = prompt_template.format(ticker=..., input_text=..., current_price=current_price, ...)`. The same pattern is at routes/gomes.py:1795 `current_price=stock.current_price or position.current_price or 0`. This is live for the four 403/KeyError tickers identified above, whose current_price is NULL.
- **Dopad na misi:** Breaks (4) and (5). The AI is told the stock trades at $0.00 and will reason from it — producing valuation and entry-zone language that is confidently wrong, in the exact screen the owner uses to make a decision. A fabricated $0 is worse than a refusal because it produces fluent, plausible output.
- **Návrh opravy:** Pass None through and have the prompt template render "AKTUÁLNÍ CENA: NEZNÁMÁ — neuváděj žádné cenové cíle ani vstupní pásma", or short-circuit the endpoint with a 409 telling the user to fix the price first. Never substitute 0 for an unknown price on the way into a model.
- **Ověřovatel:** The core claim survives verification. backend/app/routes/intelligence_gomes.py:802 still reads `current_price = float(stock.current_price or 0)` verbatim, and it flows unguarded into `prompt_template.format(...)` at :817-824. The template really does interpolate it as a price: prompts_universal_intelligence.py:31 says `Current position data: Price ${current_price}, ...`, so a N …

**33. NYSE holidays are hardcoded as bare (month, day) tuples with no year (backend/app/core/market_hours.py:38-48, matched at :102), so the table both drifts from 2027 onward AND is already wrong for 2026: …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** core/market_hours.py:37-48: `# NYSE státní svátky 2026 (pevně dané dny)` / `NYSE_HOLIDAYS_2026: Final[set[tuple[int, int]]] = { (1, 1), (1, 20), (2, 17), (4, 3), (5, 25), (7, 3), (9, 7), (11, 26), (12, 25), }` and the test at :102 `return (dt.month, dt.day) in NYSE_HOLIDAYS_2026` — no year is ever compared. Most of these float: MLK Day, Presidents' Day, Good Friday, Memorial Day, Labor Day and Thanksgiving all land on different dates in 2027.
- **Dopad na misi:** Breaks (5) quietly and on a delay. is_nyse_holiday feeds is_market_open (:145) feeds should_refresh_market_data (:219) — so from 2027 the app will believe the market is shut on ~6 normal trading days a year (skipping refreshes and serving cache) and open on ~6 real holidays. Given the app must survive long unattended s …
- **Návrh opravy:** Either compute the floating holidays from rules, or key the set by (year, month, day) and raise/log loudly when the current year has no entry — silently returning False for an unknown year is how this becomes invisible.
- **Ověřovatel:** Claim confirmed and understated. File is at backend/app/core/market_hours.py (claim cited core/market_hours.py — path prefix wrong, all line numbers exact). NYSE_HOLIDAYS_2026 at :38-48 is a set of bare (month, day) tuples and is_nyse_holiday at :102 tests `(dt.month, dt.day) in NYSE_HOLIDAYS_2026` with no year comparison — verbatim as claimed.

Every standard refutation path f …

**34. The currency service does zero caching: `_fetch_cnb_rate` issues a fresh blocking `requests.get` (timeout=5 s) on every `get_rate_to_czk` call (backend/app/services/currency.py:83, :129). `get_all_rat …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** services/currency.py:129 `response = requests.get(CNB_API_URL, timeout=REQUEST_TIMEOUT_SECONDS)` (synchronous requests, REQUEST_TIMEOUT_SECONDS = 5 at :33) is executed on every single call to get_rate_to_czk (:83) — there is no cache anywhere in the module. get_all_rates (:148-152) loops all 12 currencies, so one GET /api/currency/rates issues 12 separate HTTP fetches of the same 879-byte file (measured 0.75 s warm). Both currency routes are `async def` calling this blocking code directly on the event loop: routes/currency.py:39-45 `async def get_exchange_rates() ... rates = CurrencyService.get_all_rates()` and :50-57 `async def get_rate(currency: str) ... rate = CurrencyService.get_rate_to_ …
- **Dopad na misi:** Breaks the ~2 minutes / one screen budget and, on a slow CNB, blocks the whole FastAPI server (async def + blocking requests.get holds the event loop). Worst case for /api/currency/rates is 12 x 5 s = 60 s of a frozen backend; the daily-actions screen with 14 positions is another 14 fetches. It also raises the odds of …
- **Návrh opravy:** Fetch and parse the CNB file once, cache the whole rate table keyed by its own "21 Aug 2026 #161" date header, and serve every currency from that one parse. Make the routes `def` (so FastAPI runs them in the threadpool) or move the fetch to httpx.AsyncClient.
- **Ověřovatel:** Tried to refute it and could not. The mechanics check out at the exact cited lines (real paths are `backend/app/...`, the claim elided the prefix):

CONFIRMED:
- `backend/app/services/currency.py:129` is verbatim `response = requests.get(CNB_API_URL, timeout=REQUEST_TIMEOUT_SECONDS)`; `REQUEST_TIMEOUT_SECONDS: Final[int] = 5` at :33. No refactor.
- Zero caching. Grepped the who …

**35. Two of the five documented startup paths are stale on the port: README.md:56 and docs/AKCION_SPEC.md:164 tell him to run `cd backend && python start.py`, which hardcodes --port 8000 (backend/start.py: …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** The frontend is wired to port 8002 in all three places: frontend/.env `VITE_API_URL=http://localhost:8002`, frontend/vite.config.ts proxy `target: 'http://127.0.0.1:8002'`, and frontend/src/api/client.ts:53 `const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8002';`. Only backend/run_server.py matches it (`host="0.0.0.0", port=8002` at :24-25). But README.md:56 instructs `python start.py`, and backend/start.py:52 launches `[..., "uvicorn", "app.main:app", "--reload", "--host", "0.0.0.0", "--port", "8000"]`. README.md:59-60 then states `Backend: **http://localhost:8000**`, README.md:236 tells him to set `VITE_API_URL=http://localhost:8000`, docs/AKCION_SPEC.md:163 says `ba …
- **Dopad na misi:** This is the first thing that happens on the day he comes back. Following his own written instructions starts the API on 8000 while the UI polls 8002, so every screen is empty or erroring with ECONNREFUSED and no message explains why. That burns the entire ~2-minute / <=3-action budget on debugging infrastructure, again …
- **Návrh opravy:** Pick 8002 as the single source of truth. Change backend/start.py:52 to port 8002 (or delete start.py and make run_server.py the only entry point), fix the printed URLs in start.ps1, and correct README.md:59-60/:236, docs/AKCION_SPEC.md:163, docs/QUICKSTART.md and frontend/.env.example. Add a one-line note that the fron …
- **Ověřovatel:** Every cited line verified verbatim and the mismatch is real. backend/start.py:52 launches uvicorn with --port 8000; backend/run_server.py:24-25 uses port=8002. The frontend is wired to 8002 in all three cited places (frontend/.env VITE_API_URL=http://localhost:8002, vite.config.ts proxy target 127.0.0.1:8002, client.ts:53 default http://localhost:8002). README.md:56 does instru …

**36. Four blanket `pytestmark = pytest.mark.skip(...)` markers in backend/tests/ (test_api_endpoints.py:9, test_api_integration.py:21, test_phase1_extraction.py:15, test_yahoo_cache.py:14), all added in co …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** Four files carry a blanket `pytestmark = pytest.mark.skip(reason="Legacy API-shape tests — superseded by current suites; repair-or-delete tracked in AKCION_SPEC §7")` — tests/test_api_endpoints.py:9, tests/test_api_integration.py:21, tests/test_phase1_extraction.py:15, and tests/test_yahoo_cache.py. Re-running those files with the markers stripped gives `28 failed, 19 passed, 6 skipped, 1 error`. docs/AKCION_SPEC.md:137 honestly tracks this as "repair or delete the 28 legacy failing tests" — the failure count is accurate — but the skip is applied per-FILE, so 19 currently-passing tests are switched off alongside them. The 28 failures are genuine test rot, not app breakage: `test_health_check …
- **Dopad na misi:** Undermines point (5)/mechanical confidence: the headline `148 passed` overstates real coverage by hiding that the entire HTTP surface and the price layer are untested. Because the skip is file-wide, repairing one broken test does not restore the 19 working ones, so the coverage debt compounds silently. The prompt drift …
- **Návrh opravy:** Replace the four module-level pytestmarks with per-test `@pytest.mark.xfail(strict=True, reason=...)` on the 28 that actually fail, so the 19 passing tests run again and any repaired test flips CI red->green visibly. Add a session fixture that calls initialize_database against sqlite in-memory to clear the ~20 `Databas …
- **Ověřovatel:** The finding survives verification on every load-bearing point; I could not refute it.

1. The skips exist verbatim, at the cited lines (paths are `backend/tests/...`, not `tests/...`, but that is the only inaccuracy): C:\Users\reath\Projects\Akcion\backend\tests\test_api_endpoints.py:9, test_api_integration.py:21, test_phase1_extraction.py:15, test_yahoo_cache.py:14 — all four …

**37. An untracked, machine-local .venv in the repo root (Python 3.14.2, Streamlit-era: streamlit/altair, no fastapi) cannot run the backend — `import fastapi` raises ModuleNotFoundError. It is not committe …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** `.venv/Scripts/python.exe -c "from app.main import app"` -> `ModuleNotFoundError: No module named 'fastapi'`; `pip list` in that venv (Python 3.14.2) shows streamlit/altair/pydeck and no fastapi, uvicorn, sqlalchemy or yfinance — it is a leftover from the pre-FastAPI Streamlit app, matching the root requirements.txt (`streamlit>=1.28.0`). The backend runs only on the system Python 3.12.10 resolved from PATH; nothing in the repo pins or documents that. Separately, backend/start.py:34-37 runs `subprocess.run([sys.executable, "-m", "pip", "install", "-r", "requirements.txt"], check=True)` on EVERY launch. `pip install --dry-run -r backend/requirements.txt` exits 0 but reports it would downgrade …
- **Dopad na misi:** Against point (3)/(4) and "survive weeks of absence": returning after a month, the obvious move is to activate the .venv sitting in the repo root, which produces an immediate ModuleNotFoundError with no hint that the correct interpreter is a global one. The start.py behaviour also means the environment he tests in is n …
- **Návrh opravy:** Delete or rename the stale root .venv (and the root Streamlit requirements.txt if the Streamlit app is retired), create backend/.venv on Python 3.12 from backend/requirements.txt, and document that path in README. Remove the pip install from backend/start.py so launching never mutates the environment, and reconcile the …
- **Ověřovatel:** I attempted refutation on six angles and every one failed; the finding is substantially accurate and reproducible.

VERIFIED:
1. C:/Users/reath/Projects/Akcion/.venv is dead for the backend. `.venv/Scripts/python.exe -c "import fastapi"` -> ModuleNotFoundError. `pyvenv.cfg` shows version 3.14.2 from a separate pythoncore-3.14-64 install. pip list shows streamlit 1.53.1, altair …

**38. The real-time news layer (/api/invest/*, backed by a live Polygon.io fetch in news_monitor.py) and the 3-pillar master signal (/api/master-signal/*, /api/action-center/*) are registered in main.py but …**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** Comparing all 133 registered /api routes against the 57 distinct paths grepped from the 45 frontend .ts/.tsx files: 76 routes are never called. Among them the whole /api/invest/* router — /api/invest/news, /api/invest/news/{ticker}, /api/invest/analyze, /api/invest/analyze/{ticker}, /api/invest/alerts, /api/invest/opportunities — plus /api/master-signal/{ticker} and /api/master-signal/batch, /api/intelligence/reconcile/preview and /api/intelligence/reconcile/{id}, and all five /api/yahoo/* routes. The news layer is real code that does hit the internet: backend/app/services/news_monitor.py:162-164 opens `httpx.AsyncClient(timeout=30.0)` against `https://api.polygon.io/v2/reference/news`, keye …
- **Dopad na misi:** The closest thing the repo has to mission point (1) — a live Polygon.io news fetch with a configured key — exists, works in principle, and has no button. docs/AKCION_SPEC.md:169 names master_signal.py as a key file, yet its endpoint has no UI either. So the owner's "press Analyze" flow routes to the dead-model transcri …
- **Návrh opravy:** Wire the Analyze button to call /api/invest/news/{ticker} alongside the Gomes path and render the headlines with their published_utc timestamps, so "no news" is visibly distinct from "fetch failed". Make fetch_news_polygon raise or return an explicit error state instead of a bare [] so a dead key cannot masquerade as a …
- **Ověřovatel:** I tried to break this and could not. Every cited fact checks out at the cited lines, and the escape hatches I looked for (different mount name, lazy import, background caller, upstream service reuse) either do not exist or dead-end.

Route registration — C:\Users\reath\Projects\Akcion\backend\app\main.py:123-137 mounts investment.router (prefix /api/invest, backend/app/routes/i …

**39. An unauthenticated arbitrary-SQL endpoint with auto-commit is live in the owner's configuration, bound to 0.0.0.0**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** backend/app/routes/dev_utils.py:22-26 defines `@router.post("/execute-sql")` / `async def execute_sql(request: SQLExecuteRequest, db: Session = Depends(get_db))`, which splits the caller's string on semicolons and runs each through `db.execute(text(stmt))` (:41) followed by `db.commit()` (:58). There is no auth dependency, no allowlist, and no read-only check. It is guarded at backend/app/main.py:139-140 by `if settings.debug:` / `app.include_router(dev_utils.router)`, and `debug` defaults to False (backend/app/config/settings.py:114 `debug: bool = Field(default=False, alias="DEBUG")`) — but backend/.env:23 sets `DEBUG=True`, so on the owner's machine the route IS mounted. Both launchers bin …
- **Dopad na misi:** Against "correctness is the feature" and the real-money constraint: any device on the home LAN can POST a DELETE or UPDATE against the live Neon portfolio database and have it committed, with no authentication and no audit trail. There is no deployed public surface here — the exposure is LAN-scope, which is why this is …
- **Návrh opravy:** Bind both launchers to 127.0.0.1 instead of 0.0.0.0 — nothing in the app needs LAN exposure. Set DEBUG=False in backend/.env and drive the dev router off a separate explicit flag (e.g. ENABLE_DEV_SQL) so ordinary debug logging cannot re-arm it. If the endpoint is still needed for migrations, require a token and reject …
- **Ověřovatel:** Attempted refutation on six axes; all failed. (1) Code not refactored: backend/app/routes/dev_utils.py:22-68 is exactly as cited — POST /api/dev/execute-sql, no auth dependency, no allowlist, no read-only check, splits caller string on ';' and runs each through db.execute(text(stmt)) at :41, then db.commit() at :58. (2) Guard not upstream-closed: backend/app/main.py:139-140 mou …

**40. The Kelly allocator's request and response contracts are both broken, and the backend mislabels CZK sums as EUR. Because the widget is not mounted anywhere, none of this is currently visible to the us …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** REQUEST — frontend/src/api/client.ts:585-593 sends `params: { available_czk: availableCzk, available_eur: availableEur }`, but backend/app/routes/portfolio.py:886-888 declares `portfolio_id: int, available_eur: float = 0, additional_czk: float = 0`. FastAPI ignores unknown query params, so `available_czk` is dropped and `additional_czk` stays 0. The user types 20 000 Kč (widget default `defaultCzk = 20000`, :27) and the backend budgets zero of it.
RESPONSE — the route returns (portfolio.py:915-934) `available_capital_eur`, `available_capital_czk`, `recommendations[].recommended_amount_eur/_czk`, `conviction_score`, `current_weight_pct`, `recommended_weight_pct`, `priority`, `reasoning`, `tot …
- **Dopad na misi:** Breaks (2) how much to buy and the memory rule "never fake numbers filling gaps". This is the reason the previous finding must not be fixed by simply mounting the widget: as wired, it would confidently show him NaN totals and, if the labels were trusted, a 25× overstatement of capital deployed.
- **Návrh opravy:** Pick one contract and make both sides match. Rename the route params to `available_czk` / `available_eur` (or fix the client), rename `total_allocated_eur`→`total_allocated_czk` and `remaining_cash_eur`→`remaining_czk`, and add the fields the widget renders (`kelly_weight_pct`, `currency`, `risk_level`) or strip them f …
- **Ověřovatel:** All three sub-claims verified against current code; nothing was refactored away.

REQUEST: client.ts:582-593 sends params {available_czk, available_eur}; portfolio.py:885-889 declares (portfolio_id: int, available_eur: float = 0, additional_czk: float = 0). FastAPI ignores unknown query params, so additional_czk stays 0. kelly_allocator.py:131 computes total_available_czk = (av …

**41. kelly_allocator.py hardcodes CZK_EUR_RATE = 25.0 (:99) and applies it uniformly to every position (:141, :166) without ever reading Position.currency, even though CurrencyService.get_rate_to_czk (curr …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/services/kelly_allocator.py:98-99 — `# Default exchange rate (should be fetched dynamically)` / `CZK_EUR_RATE = 25.0`. The comment admits the defect.
It is then applied blindly to every holding: :140-143 `portfolio_value_czk = sum((p.current_price or p.avg_cost) * p.shares_count * self.CZK_EUR_RATE for p in positions)` and :166 `current_value_czk = (position.current_price or position.avg_cost) * position.shares_count * self.CZK_EUR_RATE`. The acknowledgement is inline at :139-140: `# Note: In real scenario, we'd convert each position's currency`.
`Position.currency` exists and is populated (backend/app/models/portfolio.py:167; backend/app/routes/daily_actions.py:64 reads `currenc …
- **Dopad na misi:** Breaks (5) mechanical/systematic and the memory rule that a fake number can cost real money. Every weight, gap and CZK recommendation this service produces is scaled by an invented constant. It is currently unreachable, so it costs nothing today — but it is the module someone will reach for when fixing the previous two …
- **Návrh opravy:** Delete `CZK_EUR_RATE` and inject `CurrencyService.get_rate_to_czk`, converting per position via `p.currency`. Guard the null case: skip positions where both `current_price` and `avg_cost` are None and surface them as an explicit warning in the AllocationPlan rather than crashing or silently valuing them at zero.
- **Ověřovatel:** The cited code exists verbatim and the core defect is real, so the finding survives — but the claimed mechanism and impact are substantially wrong.

VERIFIED TRUE: kelly_allocator.py:98-99 is exactly `# Default exchange rate (should be fetched dynamically)` / `CZK_EUR_RATE = 25.0`. Lines 140-143 and 166 apply it to every position. `grep -n "currency" backend/app/services/kelly_ …

**42. `RiskRewardCalculator.three_point_up`/`three_point_down` (backend/app/trading/gomes_logic.py:606-637), `should_take_profit` (:640-650) and `apply_doubling_rule` (:653-676) have zero production callers …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/trading/gomes_logic.py:606-620 `three_point_up` — `return current_price * (high / low) ** (3 / (10 - top_score))` — and :622-637 `three_point_down` — `return current_price / (high / low) ** (3 / (10 - top_score))`. I verified the algebra: substituting into the score formula at :536 gives exactly score∓3, and these match docs/GOMES_METHODOLOGY_CANON.md:130-131 character for character.
`grep -rn "three_point_up\|three_point_down"` across backend/ and frontend/src returns SIX hits: the two definitions (:606, :623) and four in backend/tests/test_rr_scoring.py (:122, :125, :130, :133). Zero callers in any route, service, or component.
The canon calls this the resolution of its own ope …
- **Dopad na misi:** Breaks (2) advise WHEN, and (3)/(4) — a pre-computed "sell at $X / add at $Y" is precisely the mechanism that lets someone with MS relapses act on a limit order set weeks earlier instead of reacting emotionally to a price move. The math is done and correct; it is simply not plugged into any output.
- **Návrh opravy:** Add `three_point_up(price, green, red)` and `three_point_down(...)` to the ActionItem payload as `take_profit_price` and `add_price`, and render them in DailyActionWidget next to the quantity line (frontend/src/components/DailyActionWidget.tsx:199-203). Two extra numbers, zero new screens, and they turn every recommend …
- **Ověřovatel:** I re-read gomes_logic.py:520-700 — the cited lines are current and say exactly what the claim quotes. I re-ran the grep across all of backend/ and frontend/src (py/ts/tsx/js, node_modules excluded): the only hits are the four definitions and four test references, so there are genuinely no callers. I checked for the usual escape hatches and found none — no inline reimplementatio …

**43. When CNB is unreachable, share counts and all CZK valuations fall back to a hardcoded rate table stamped 2025-01-11 with no staleness flag anywhere in the return type, the response schema, or the UI. …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/services/currency.py:87-93 — `except Exception as e:` / `logger.warning(f"Failed to fetch live rate for {currency}, using fallback: {e}")` / `fallback = cls.FALLBACK_RATES.get(currency, cls.FALLBACK_RATES["USD"])` / `return fallback`. The failure is logged server-side and then returned as an ordinary float; the caller cannot distinguish live from fallback.
The table is stamped :43 `# Fallback rates (updated 2025-01-11)` — today is 2026-08-22, so 19 months stale. Note :91 also silently substitutes the USD rate for any currency not in the table.
That float becomes the share count with no staleness flag: backend/app/services/daily_actions.py:386-388 `rate = fx_rate_to_czk("USD")` / …
- **Dopad na misi:** Breaks the non-negotiable "survive weeks of absence with honest stale-data labelling and never fake numbers filling gaps". If he opens the app after an MS relapse and CNB is down or the network is flaky, he gets a confident share count derived from a 2025 exchange rate and nothing on screen says so.
- **Návrh opravy:** Have `get_rate_to_czk` return `(rate, is_live, as_of)` (or raise on failure and let the caller decide). In `generate_daily_actions`, append a warning like the existing ones — "⚠️ STARÝ KURZ: USD/CZK z {date}, ověř před obchodem" — whenever any action's quantity was computed from a fallback rate. Reuse the `_grouped` he …
- **Ověřovatel:** I tried to break this claim on every listed axis and could not.

VERIFIED AS CITED:
- C:\Users\reath\Projects\Akcion\backend\app\services\currency.py:43 still reads `# Fallback rates (updated 2025-01-11)`. Lines 87-93 match the quoted evidence verbatim, including line 91's `cls.FALLBACK_RATES.get(currency, cls.FALLBACK_RATES["USD"])` USD substitution. `get_rate_to_czk` returns …

**44. No Away/Flare mode exists in code: there is no absence detection (no last_login/last_seen anywhere in backend/app/models), no risk-posture parameter (backend/app/services/daily_actions.py:82-88 and ba …**  
*Oblast: Časování a automatika*

- **Důkaz:** `grep -rniE "away.?mode|flare|absence|dormant|vacation|pause.?mode|holiday.?mode|nepritom|nepřítom|pauza"` across all `.py`, `.ts`, `.tsx` in the repo returns ZERO code hits. Every hit is documentation stating it is missing:
- `docs/AKCION_SPEC.md:125`: `| 7. Away mode: tighter stops, raise-cash default, single most-urgent push alert | set once | ❌ not built |`
- `docs/AKCION_SPEC.md:109`: "...no monthly-deposit deployment path, **no Away mode**, notifications are email-only"
- `docs/AKCION_SPEC.md:137`: "4. **Path 7: Away mode** + single-alert push channel (Telegram or email digest)."
- `docs/EFFICIENT_INVESTING_PLAYBOOK.md:71`: "### Path 7 — 'Away mode' (Delší nepřítomnost) ⏱ nastavit jedn …
- **Dopad na misi:** Breaks mission points (4) and (5) and the explicit project-memory requirement "the app must survive weeks of absence (MS relapses)". Today an absence of six weeks and an absence of six hours produce byte-identical behaviour: same sizing, same thresholds, same silence. There is no raise-cash default, no tightened stop, …
- **Návrh opravy:** Minimum viable version, one screen: persist `owner_last_active_at` (bump it in the `/api/trading/daily-actions` handler) plus an explicit `away_mode` boolean + `away_until` on a settings row. Thread a `posture: NORMAL|AWAY` argument into `generate_daily_actions` that (a) blocks all BUY candidates outright at `_buy_acti …
- **Ověřovatel:** The core claim survives. Grep over backend/ and frontend/src (excluding .venv and node_modules) for away.?mode|flare|absence|dormant|vacation|pause.?mode|nepřítom|pauza|away_ returns zero code hits, and last_login|last_seen|last_visit|days_away|seen_at returns zero. backend/app/services/daily_actions.py:82-88 confirms generate_daily_actions(market_alert, positions, analyses, ca …

**45. The scheduler's market-hours gate uses naive local wall-clock time with no timezone, weekday, or holiday check — on this Czech-time host it reports "market open" on a Saturday at 05:15 ET, and its 09: …**  
*Oblast: Časování a automatika*

- **Důkaz:** `backend/app/services/alert_scheduler.py:39-52`:
```
39: MARKET_OPEN_HOUR = 9  # 9:00 AM
40: MARKET_CLOSE_HOUR = 16  # 4:00 PM
...
47: def is_market_hours() -> bool:
48:     """Check if current time is during market hours (9 AM - 4 PM)"""
49:     now = datetime.now().time()
50:     market_open = time(MARKET_OPEN_HOUR, 0)
51:     market_close = time(MARKET_CLOSE_HOUR, 0)
52:     return market_open <= now <= market_close
```
`datetime.now()` is the server's local wall clock. No `weekday()` check, no holiday check, no tz. It gates the only work the loop does (`alert_scheduler.py:86`: `if is_market_hours(): await run_alert_check()`).

`backend/app/core/market_hours.py:105-162` already implements …
- **Dopad na misi:** Breaks mission points (1) and (5). The timing primitive that decides WHEN the app looks at the market is wrong in three directions at once — wrong timezone, weekends treated as trading days, holidays ignored. It is currently masked by Finding 1 (the checks are no-ops), so repairing notifications without repairing this …
- **Návrh opravy:** Delete `is_market_hours()` from `alert_scheduler.py` and `from app.core.market_hours import is_market_open`, calling `is_market_open(include_extended_hours=True)` at line 86. Also fix `core/market_hours.py:38-48` / `:102` while you are there — `NYSE_HOLIDAYS_2026` is a hardcoded `set[tuple[int, int]]` matched by `(dt.m …
- **Ověřovatel:** The finding survives every refutation vector I tested.

VERBATIM CONFIRMED: backend/app/services/alert_scheduler.py:39-52 reads exactly as cited — MARKET_OPEN_HOUR=9, MARKET_CLOSE_HOUR=16, and is_market_hours() using `datetime.now().time()` with no tzinfo, no weekday() call, no holiday lookup. Line 86 (`if is_market_hours(): await run_alert_check()`) is the only gate on the loo …

**46. An action tells the owner WHAT and HOW MUCH but never WHEN: ActionItem has no deadline/valid-until field, urgency_score is a pure relative sort key, and the one structured catalyst date the DB can hol …**  
*Oblast: Časování a automatika*

- **Důkaz:** `backend/app/schemas/daily_actions.py:16-31` — the entire contract for an executable instruction:
```
19:    id: str
20:    ticker: str
21:    source_key: str
22:    action_type: str
23:    current_price: float
24:    currency: str = "USD"
25:    target_price: float | None = None
27:    quantity: float
28:    estimated_czk_value: float
29:    reason: str
30:    urgency_score: int  # higher = do first
31:    review_required: bool = False
```
`urgency_score` is a relative sort key only — `services/daily_actions.py:209-210` `candidates.sort(key=lambda a: (-a.urgency_score, a.ticker)); actions = candidates[:MAX_ACTIONS]`. There is no date, deadline, valid-until, or act-by field anywhere in the p …
- **Dopad na misi:** Directly undercuts mission point (2), which is literally "how much to buy, how much to sell, and **WHEN**". Today the answer to WHEN is always the implicit "right now, today" — the widget header is `CO MÁM DNES UDĚLAT` (`DailyActionWidget.tsx:172`). After a two-week gap he reopens the app and gets three actions with no …
- **Návrh opravy:** Add `act_by: date | None` and `first_seen_at: datetime` to `ActionItem`; carry `catalyst_date` / `next_catalyst` through `AnalysisInput` and `load_daily_action_inputs`, and set `act_by` from the catalyst for BUY/TRIM candidates. Render "platí od {first_seen_at}" on each card and a "Naposledy spočítáno: {generated_at}" …
- **Ověřovatel:** All cited lines verified verbatim. schemas/daily_actions.py:16-31 ActionItem has no date/deadline/valid-until field; services/daily_actions.py:209-210 is exactly `candidates.sort(key=lambda a: (-a.urgency_score, a.ticker)); actions = candidates[:MAX_ACTIONS]` and urgency_score comes from constant bands (lines 47-52), consumed only by that sort — it never touches a clock. Analys …

### P3 — kosmetika

**47. force_refresh is a no-op throughout the Gomes analysis stack, not just on the GET cache-hit path. GET /api/gomes/analyze/{ticker} (backend/app/routes/gomes.py:208-292) returns the stored Stock row whe …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/routes/gomes.py:208-211 `@router.get("/analyze/{ticker}") def analyze_ticker_simple(ticker: str, force_refresh: bool = Query(False, description="Force new ML prediction"), …)`. The parameter is then never read on the hot path: :231 `if stock and stock.conviction_score is not None:` returns the stored row at :257-291; `force_refresh` is only passed through at :288 `score = analyzer.analyze_ticker(ticker=ticker_upper, force_refresh=force_refresh)`, inside the branch reached ONLY when the ticker has no stored conviction_score. Caller: frontend/src/components/AnalyticsDashboard.tsx:48 `const score = await apiClient.gomesAnalyzeTicker(ticker, true); // force refresh`.
- **Dopad na misi:** Breaks (5) mechanical/systematic. A parameter named force_refresh that does nothing is exactly the class of silent no-op the project memory flags as trust-breaking. Mitigating fact, stated honestly: AnalyticsDashboard.tsx is dead code today (nothing imports it — App.tsx renders only InvestmentTerminal), so no user curr …
- **Návrh opravy:** In routes/gomes.py:231, change the guard to `if stock and stock.conviction_score is not None and not force_refresh:` so the fallback analyzer path at :283-290 actually runs on refresh — or delete the parameter and rename the endpoint to /cached-score/{ticker} so its contract is honest.
- **Ověřovatel:** I tried each of the standard refutation angles and none of them landed. (1) Code is current: gomes.py:208-211 and :231 read verbatim as cited; the stored-row return at :262-280 contains no reference to force_refresh, and the only pass-through is at :289-292 inside the no-stored-score branch. (2) Not a stale citation: nothing has been refactored. (3) The "dead component" really …

**48. Three stale string literals in diagnostic-only code paths: /api/analyze/health (backend/app/routes/analysis.py:520-526) hardcodes "model": "gemini-3-pro-preview" and a "google_search" feature while th …**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/routes/analysis.py:520-526: `return { "status": "healthy", "service": "analysis", "model": "gemini-3-pro-preview", "features": ["google_search", "aggressive_extraction", "gomes_rules"], }` — both strings are hardcoded literals. The real model is `core/constants.py:143 model_name: str = "gemini-2.0-flash"` and google_search is `core/prompts.py:262 GOOGLE_SEARCH_CONFIG: Final[None] = None`. Also backend/app/services/gomes_deep_dd.py:73-75 `self.model = genai.GenerativeModel('gemini-2.0-flash')` … `logger.info("Gemini 2.5 Pro initialized for Deep Due Diligence")` — the log line names a third model. And backend/tests/test_phase1_extraction.py:55 `assert GEMINI_MODEL_NAME == "gemini-3 …
- **Dopad na misi:** Breaks (5) and the "correctness is the feature" rule at the diagnostic layer. When he or anyone helping him asks the app what it is doing, /api/analyze/health answers with a model it does not call and a `google_search` capability that is hardcoded to None — the single most load-bearing false claim in the codebase given …
- **Návrh opravy:** Make the health endpoint report facts: `"model": GEMINI_CONFIG.model_name` and derive `features` from actual state (`"google_search" if GOOGLE_SEARCH_CONFIG is not None else …`). Fix the log string at gomes_deep_dd.py:75 to interpolate the model name. Update test_phase1_extraction.py:55 to assert against constants.GEMI …
- **Ověřovatel:** The string-drift core of the finding is factually accurate and I could not break it: analysis.py:520-526 does return hardcoded "model": "gemini-3-pro-preview" and a "google_search" feature flag; the real model is constants.py:143 "gemini-2.0-flash" (used by every actual call site — gomes.py:938, intelligence_gomes.py:829, gomes_deep_dd.py:73, knowledge_synthesis.py:121); prompt …

**49. `backend/app/core/gomes_compliance.py` is functional but unreferenced: its only two callers are `/api/trading/validate-order` and `/api/trading/order`, which no client invokes. This is dead code plus …**  
*Oblast: Emoční brzdy*

- **Důkaz:** backend/app/routes/trading.py:70 `@router.post("/validate-order", response_model=OrderValidationResponse)` and trading.py:110 `@router.post("/order")`. Full-repo grep for `validate-order|trading/order|placeOrder|validateOrder` over frontend/src and backend/app returns exactly one hit: the route definition itself at trading.py:70. The only /api/trading endpoint the client knows is frontend/src/api/client.ts:822 `'/api/trading/daily-actions'`. trading.py:127 also admits `# TODO: Integrate with actual broker API`.
- **Dopad na misi:** The entire 'circuit breaker' layer (RULE A market RED -> 403, RULE B runway<6m -> 422, RULE C Stage 4 -> 422) is decoration. Anyone reading gomes_compliance.py:5 ('circuit breakers that cannot be bypassed via frontend') would believe the app is protected. It is not. False confidence in a safety system is worse than no …
- **Návrh opravy:** Either delete trading.py:70-137 and gomes_compliance.py so nobody is misled, or wire verify_gomes_compliance as a FastAPI dependency on portfolio.py:202 add_position and portfolio.py:755 create_position (require_gomes_compliance at gomes_compliance.py:217 exists for precisely this and is also uncalled).
- **Ověřovatel:** The narrow factual claim is CONFIRMED — I verified every escape hatch and none applies.

Verified facts:
- Full-repo grep for `validate-order|trading/order` excluding node_modules/.git/__pycache__ returns exactly ONE hit: the route definition at `C:\Users\reath\Projects\Akcion\backend\app\routes\trading.py:70`. No tests, no docs, no second frontend (repo root has only one `fron …

**50. RULE D's 3% low-conviction cap is advisory, not enforced (backend/app/core/gomes_compliance.py:191-200). Unlike rules A/B/C, which raise 403/422, RULE D returns `passed=True` with `max_allocation=3.0` …**  
*Oblast: Emoční brzdy*

- **Důkaz:** backend/app/core/gomes_compliance.py:191-200 — RULE D, the only rule that does not raise:
```
        # RULE D: Low Conviction (Warning, not blocking)
        if stock.conviction_score is not None and stock.conviction_score < 7:
            logger.info(f"⚠️ ORDER WARNING: {ticker} - Low conviction score ({stock.conviction_score}/10)")
            return ComplianceResult(
                passed=True,
                ...
                max_allocation=3.0,  # Max 3% portfolio allocation
            )
```
`max_allocation` is returned to the caller and, per Finding 2, no caller exists; even in trading.py:94-100 the value is only echoed into the JSON response, never checked against the requested …
- **Dopad na misi:** Even if the compliance layer were wired up (Findings 1-2), the size cap would remain a suggestion — the order that violates it still returns allowed=true. Worth fixing only after the layer is actually reachable, hence P3.
- **Návrh opravy:** Compare the requested notional against max_allocation × portfolio value inside verify_gomes_compliance and raise 422 on breach, so the cap is enforced at the same choke point as rules A/B/C rather than reported alongside them.
- **Ověřovatel:** The core defect is real and the citation is accurate, but the finding's supporting premise is factually wrong.

WHAT HELD (verified):
1. `c:/Users/reath/Projects/Akcion/backend/app/core/gomes_compliance.py:191-200` matches the quoted evidence verbatim. RULE D is indeed the only rule of A-D that returns instead of raising (A raises 403 at :138, B raises 422 at :158, C raises 422 …

**51. In the Polygon/Massive OHLCV sync (backend/app/trading/data_fetcher.py:127), when the provider's bar omits the 'vw' field the code falls back to writing the typical price (H+L+C)/3 into the nullable ` …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** trading/data_fetcher.py:127: `vwap=float(r.get('vw', (r['h'] + r['l'] + r['c']) / 3))` inside the OHLCVData construction (:119-128). The typical price (H+L+C)/3 is not a volume-weighted average price; it is written into a column named vwap with no marker distinguishing it from Polygon's real 'vw' field, and the row is committed at :132.
- **Dopad na misi:** Breaks the never-fake-a-number rule at the storage layer, which is the worst place for it — once written, nothing downstream can tell a real VWAP from a computed one. Currently limited-blast because /api/trading/sync/data is UI-unreachable (finding 9), but any future indicator reading OHLCVData.vwap inherits a silent f …
- **Návrh opravy:** Store NULL when Polygon omits 'vw'. If a typical-price proxy is genuinely wanted, put it in a separate, differently named column so a consumer must opt in.
- **Ověřovatel:** The cited line survives verbatim at backend/app/trading/data_fetcher.py:127 (claim's path omitted the backend/app/ prefix), inside the OHLCVData construction at :119-128, committed at :132. So the code fact holds and I cannot refute it outright. However two material parts of the claim are wrong. (1) It is a dict.get FALLBACK, not unconditional synthesis: when the provider retur …

**52. check_entry_zones() in backend/app/services/news_monitor.py:292-299 reads the newest OHLCVData close with no staleness check and labels the resulting alert ACTION_REQUIRED (:311) — a real missing guar …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** services/news_monitor.py:292-299: `latest_price = self.db.query(OHLCVData).filter(OHLCVData.ticker == item.ticker).order_by(OHLCVData.time.desc()).first()` / `if not latest_price: continue` / `current_price = float(latest_price.close)`. There is no comparison of latest_price.time against now. That value becomes the alert body at :310 `message=f"{item.ticker} at ${current_price:.2f} - IN ENTRY ZONE (${entry_price:.2f})..."` with `urgency=NewsUrgency.ACTION_REQUIRED` (:311). The OHLCVData table is filled only by DataFetcher via POST /api/trading/sync/data, which the frontend never calls (finding 9), so the newest row can be arbitrarily old.
- **Dopad na misi:** Breaks (4). An ACTION_REQUIRED alert saying a stock is in the buy zone, computed from a months-old close, is a direct push toward a bad trade — and it is the highest-urgency label the system has.
- **Návrh opravy:** Reject any OHLCV row older than a hard bound (1-2 trading days) and emit "cena pro {ticker} je stará {n} dní — pásmo nevyhodnocuji" instead of an alert. Include latest_price.time in the PriceAlert payload and render it in the message.
- **Ověřovatel:** Code-level claim verified exactly. news_monitor.py:292-299 reads the newest OHLCVData row by time with no staleness comparison, and :310-311 builds an ACTION_REQUIRED message from it. Same unguarded pattern at investment_engine.py:140-144. OHLCVData is written only by data_fetcher.py:119 via POST /api/trading/sync/data.

But the mission impact is refuted. The alert has no deliv …

**53. yahoo_cache.py:413 uses a bare `except:` (ruff E722) around the raw_data serialisation — the only bare except in the data layer. Its blast radius is nil: raw_data is write-only (written at :412/:414, …**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** yahoo_cache.py:410-414: `try:` / `import json` / `data["raw_data"] = json.dumps(stock.info) if stock.info else None` / `except:` / `data["raw_data"] = None` — a bare `except:` (the only one in the data layer; verified by grep across yahoo_cache.py, market_data.py, currency.py, data_fetcher.py and news_monitor.py) that catches KeyboardInterrupt and SystemExit too. Separately, `stock.info` is referenced at :341, :370, :394 and :412 within a single _fetch_and_cache_data call.
- **Dopad na misi:** Minor on its own, but it is on the same code path as finding 1: the bare except is one more place a real failure produces a quiet None and the function still returns True at :434. The repeated .info access is defensive-cost only (yfinance caches per Ticker instance) but obscures how many network round-trips a refresh a …
- **Návrh opravy:** Replace `except:` with `except Exception as e: logger.warning(...)`. Hoist `info = stock.info` once at the top of the try and reuse it across the market/fundamental/financial/raw_data blocks.
- **Ověřovatel:** The bare except is real and quoted verbatim: yahoo_cache.py:410-414 contains `try: / import json / data["raw_data"] = json.dumps(stock.info) if stock.info else None / except: / data["raw_data"] = None`. A grep for `except\s*:` across the data-layer services returns exactly one hit (line 413), so "the only one in the data layer" holds (though the cited data_fetcher.py does not e …

**54. The AGREE position cap of 15% (gomes_logic.py:1211) is unreachable dead code, and its value contradicts the canon's 10% Primary ceiling. At gomes_logic.py:1283, `min(max(tier_max_pct, 0.0), cap)` can …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/trading/gomes_logic.py:1283 — `max_position_pct=min(max(tier_max_pct, 0.0), cap)`. The only possible `tier_max_pct` values come from `TIER_LIMITS` at :737, :745, :753: `10.0`, `5.0`, `2.0`. Since 10.0 < 15.0, `AGREEMENT_POSITION_CAPS["AGREE"] = 15.0` (:1211) is unreachable for every real tier — `min(10, 15)` is always 10.
The repo's own test proves it can only be hit with a fabricated tier: backend/tests/test_buy_guard.py:147-148 `d = evaluate_dual_source_buy(True, "ok", "BULLISH", tier_max_pct=20.0)` / `assert d.max_position_pct == AGREEMENT_POSITION_CAPS["AGREE"] == 15.0` — 20.0 is not a value any tier produces. The realistic test one line up (:139-142, `tier_max_pct=10.0`) ass …
- **Dopad na misi:** Breaks (5) mechanical/systematic. Not currently costing money — the binding constraint is the correct 10% — but it is a dead constant that reads as policy. Anyone raising a tier cap later would silently unlock a 15% single-name position, above the canon ceiling, with a green test suite.
- **Návrh opravy:** Either lower AGREE to 10.0 so it matches canon §6 and the doctrine is stated once, or delete the AGREE entry and let the tier cap stand alone for the agreeing case (SINGLE/MIXED 7 and CONFLICT 5 are the only entries that actually do work). Replace the `tier_max_pct=20.0` test with one that uses a real tier value.
- **Ověřovatel:** The claim survives every refutation angle I tried.

VERIFIED: gomes_logic.py:1283 still reads `max_position_pct=min(max(tier_max_pct, 0.0), cap)` verbatim. There is exactly ONE production caller (backend/app/services/daily_actions.py:379), and it sources tier_max at :376 from `PositionSizingEngine.get_position_limit(tier, ticker).max_portfolio_pct`, which returns `config["max_p …

**55. Rule 5 ("Bubble Territory" SELL) at backend/app/core/gomes_logic.py:212-214 is unreachable: Rule 4 at :209-210 returns HOLD for the strictly weaker condition `current_price > price_target_24m`, so no …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/core/gomes_logic.py:208-214, in order:
  `# Rule 4: HOLD/SELL - Upside Realized`
  `if price_target_24m is not None and current_price > price_target_24m:`
  `    return ActionSignal.HOLD, "Target Price Achieved - Consider Profits"`
  `# Rule 5: SELL - Bubble Territory`
  `if price_target_24m is not None and current_price > price_target_24m * 1.5:`
  `    return ActionSignal.SELL, "Bubble Territory (>150% of target)"`
Any price satisfying Rule 5's condition (`> target * 1.5`) also satisfies Rule 4's (`> target`), and Rule 4 returns first. Rule 5 is therefore unreachable for all inputs. The SELL signal it was written to produce never fires; a stock at 3× its 24-month target reports …
- **Dopad na misi:** Breaks (4) stop emotional mistakes. The one rule in this engine specifically written to force a sale when a position runs away from fair value can never fire, and it fails in the permissive direction — HOLD instead of SELL, and still flagged safe to buy.
- **Návrh opravy:** Swap the two blocks so the bubble check (>150%) is evaluated before the target-achieved check (>100%). Add a regression test at the boundary — `price_target_24m=10, current_price=16` must return SELL, `current_price=12` must return HOLD.
- **Ověřovatel:** The core code claim survives verbatim. `C:\Users\reath\Projects\Akcion\backend\app\core\gomes_logic.py:208-214` reads exactly as cited: Rule 4 returns `ActionSignal.HOLD` on `current_price > price_target_24m`, and Rule 5's `current_price > price_target_24m * 1.5` is a strict subset of that condition. Rule 5 is unreachable for every input, including edge cases I checked (target …

**56. GET /api/intelligence/ml-stocks would return 500 for any watchlist stock that has both a non-NULL price_lines.current_price and at least one line, because gomes_intelligence.py:668-671 unpacks get_act …**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/services/gomes_intelligence.py:668-671 — `if current_price and (green_line or red_line):` / `price_zone, price_position_pct = RiskRewardCalculator.get_action_zone(current_price, green_line, red_line)`.
But `get_action_zone` returns a `(zone, reason)` tuple, not a percentage — backend/app/trading/gomes_logic.py:695-696 documents `Returns: (zone: "BUY"/"HOLD"/"SELL"/"UNKNOWN", reason)` and :714 returns `"BUY", f"R/R score {score:.1f}/10 (toward Green Line, undervalued)"`. So `price_position_pct` receives a Czech/English sentence.
That string is fed to a float field at backend/app/routes/intelligence_gomes.py:643 `price_position_pct=s.get("price_position_pct"),` against backend/app/ …
- **Dopad na misi:** Breaks (1) real-time information gathering as a delivery surface — this is the endpoint that feeds the ML/price-lines stock list. It is a latent landmine: the first component wired to it gets a hard 500 rather than a degraded view, and the error message will point at pydantic, not at the mis-unpacked tuple.
- **Návrh opravy:** Change gomes_intelligence.py:669 to `price_zone, _ = RiskRewardCalculator.get_action_zone(...)` and compute the percentage from the score instead — `score = RiskRewardCalculator.calculate_rr_score(current_price, green_line, red_line)` then `price_position_pct = None if score is None else round((10 - score) * 10, 1)`. T …
- **Ověřovatel:** The claim survives on every technical point; I could not break it.

Verified: gomes_logic.py:679-684 declares `-> tuple[str, str]` and every one of its seven return statements (:699, :707, :709, :710, :714, :716, :717) yields a string as the second element — there is no numeric second element anywhere in the function. gomes_intelligence.py:668-671 still reads verbatim `price_zo …

---

## Neověřeno — ber jako hypotézy

Verifikace k nim nedoběhla. Vzhledem k tomu, že u ověřených nálezů bylo zhruba čtvrtina falešných, **tohle nejsou fakta.** Ověř před použitím.

### P0 — ohrožuje peníze

**1. LIVE: the shipped app has no price-refresh path at all, and never shows how old a price is**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** `apiClient.refreshPrices` (frontend/src/api/client.ts:278) is called from exactly one place — frontend/src/components/PortfolioPage.tsx:205 — which is dead code (imported by nobody). The complete list of API calls in the live terminal (InvestmentTerminal.tsx:2898-2942, 3208-3217, 4622-4671) contains no refresh call. Server-side, backend/app/routes/portfolio.py:107-165 `get_portfolio_summary` reads the DB only — no MarketDataService call — despite docs/PORTFOLIO_PL_CALCULATION.md:11-18 claiming 'Backend automaticky: ... Zavolá Yahoo Cache pro aktuální ceny'. The only refresh triggers are CSV upload (portfolio.py:551, :663) and POST /api/portfolio/refresh (:696). backend/app/services/alert_sch …
- **Dopad na misi:** Every price, every P/L, every weight and every 'how much to buy' figure in the live UI is frozen at the moment of the last CSV import, with no age label anywhere. For a user whose stated constraint is disappearing for weeks at a time, this is the single most dangerous freshness gap: he reopens the app after a relapse, …
- **Návrh opravy:** Add a refresh button to InvestmentTerminal wired to apiClient.refreshPrices(), and render `last_price_update` next to every price (relative age, amber past 24h, red past 3 days). Fix or delete the false claim in docs/PORTFOLIO_PL_CALCULATION.md:11-18.

**2. LIVE: the monthly investing budget is fabricated as 20 000 CZK by the backend, and it sizes every buy recommendation**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/routes/portfolio.py:83 — `"monthly_contribution": portfolio.monthly_contribution or 20000.0,`. Note `or`, not `is None` — an explicitly-set 0 also becomes 20 000. Frontend mirrors it: InvestmentTerminal.tsx:87 `const DEFAULT_MONTHLY_CONTRIBUTION = 20000;` used at :2973 `totalMonthlyContribution += portfolio.portfolio.monthly_contribution ?? DEFAULT_MONTHLY_CONTRIBUTION;`. That total becomes `remainingBudget` for the allocator at InvestmentTerminal.tsx:3080-3105 which sets `pos.optimal_size = Math.round(allocation);`, rendered as a hard instruction at :862-864 `{formatCurrency(position.optimal_size)}`, at :3709 `{pos.ticker}: {formatCurrency(pos.optimal_size)}` and inside StockDet …
- **Dopad na misi:** Mission point (2) is 'advise HOW MUCH to buy'. The app answers with money he never told it he has. A fresh portfolio with no contribution configured is told to deploy 20 000 CZK/month across specific tickers, in specific amounts, presented with no hedge. If he follows it he over-invests real family money; if it is sile …
- **Návrh opravy:** Return `monthly_contribution: None` when unset and make the UI ask for it before producing any optimal_size. Change `or 20000.0` to `if portfolio.monthly_contribution is not None else None`.

**3. LIVE: FX fallback rates are 19 months stale, and an unknown currency silently gets the USD rate**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/services/currency.py:43-57 — `# Fallback rates (updated 2025-01-11)\n    FALLBACK_RATES: dict[str, float] = {"CZK": 1.00, "USD": 22.50, "EUR": 24.80, "GBP": 28.50, ...}` (today is 2026-08-22). currency.py:87-93 — `except Exception as e:\n    logger.warning(f"Failed to fetch live rate for {currency}, using fallback: {e}")\n...\nfallback = cls.FALLBACK_RATES.get(currency, cls.FALLBACK_RATES["USD"])`. The response schema carries no live/fallback flag: backend/app/routes/currency.py:16-19 `class ExchangeRatesResponse(BaseModel): rates: dict[str, float]; base: str = "CZK"`. `get_rate_to_czk` also hits the CNB HTTP endpoint on every single call (currency.py:83, no cache), and backend/a …
- **Dopad na misi:** Every CZK figure in the app — Total AUM, cost basis, P/L, gap analysis, daily BUY sizing (daily_actions.py:145-146, :386-388) — can be computed from a 19-month-old rate or, for an unlisted currency like PLN or DKK, from the USD rate, and nothing anywhere says so. Breaks 'never fake numbers filling gaps'. A single inter …
- **Návrh opravy:** Return `(rate, source, as_of)` from CurrencyService, cache the CNB fetch for the day, propagate the flag to the API response, and render 'kurz z 2025-01-11' next to any CZK figure derived from a fallback. Return None for a currency not in the table instead of substituting USD.

**4. LIVE: the BUY path in the daily action engine uses a price that has no timestamp anywhere in the schema, and hardcodes USD**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/services/daily_actions.py:354-356 — `price = analysis.current_price\n    if price is None or price <= 0:\n        return None  # no price -> no invented BUY`. That value comes from `Stock.current_price` (backend/app/routes/daily_actions.py:111 `current_price=stock.current_price`), and backend/app/models/stock.py:319-323 defines `current_price = Column(Float, nullable=True, doc="Current market price (live or last close)")` with NO companion timestamp — the model's only DateTime columns are `created_at`/`updated_at` (stock.py:66-77) and `last_review_requested` (:395). The staleness collectors in daily_actions.py:118-121 (`no_price`, `undated_price`, `stale_price`) run only over hel …
- **Dopad na misi:** The single most consequential screen ('CO MÁM DNES UDĚLAT') can print 'BUY 300 ks × 1.50 USD ≈ 10 350 Kč' from a quote of unknown age with zero warning, because the freshness guard covers only positions he already owns. And for a EUR/GBP/HKD-listed name the price is mislabelled USD and the CZK amount is wrong by the cr …
- **Návrh opravy:** Add `price_updated_at` to the Stock model, plumb it into AnalysisInput, and apply the same STALE_PRICE_AFTER/undated checks to BUY candidates before emitting an action. Carry the stock's real currency instead of the "USD" literal.

**5. /api/health hardcodes "healthy" with no database check, and startup only prints a warning when the DB fails — the app boots and reports green with no database**  
*Oblast: Provozuschopnost a důvěra*

- **Důkaz:** backend/app/main.py:154 docstring says `Check API health and database connectivity.` but the body (backend/app/main.py:157-160) is `return HealthCheckResponse(status="healthy", message="API is operational", timestamp=datetime.utcnow())` — a literal, with no db session, no query, no dependency injected. Startup does not gate on the DB either: backend/app/main.py:95-97 `success, error = initialize_database(settings.database_url)` / `if not success:` / `print(f"WARNING: Database initialization failed: {error}")` — it prints and continues. The consequence is proven by the un-skipped legacy run, where request-time calls then blow up with `RuntimeError: Database not initialized. Call initialize_da …
- **Dopad na misi:** Directly violates the non-negotiable "honest stale-data labelling, never fake numbers filling gaps" and point (5). DATABASE_URL points at a *.neon.tech host; Neon suspends idle projects, which is exactly the state after ~4 weeks dormant. On the first launch back the app can start, answer /api/health with "healthy", and …
- **Návrh opravy:** Make /api/health execute `SELECT 1` through get_db and return 503 with the driver error text when it fails; add fields for db_connected, last_price_update age, and the resolved Gemini model id so one call answers "is anything I'm about to read real?". Either abort startup on DB failure or set a module-level degraded fl …

### P1 — blokuje misi

**6. A registered endpoint labelled "AI Analyst" is wired to a mock that returns hardcoded financials ($25.5M cash, 12-month runway, "Q2 2026 Production Ramp")**  
*Oblast: Tok „Analyzovat“*

- **Důkaz:** backend/app/routes/gomes.py:1298 `@router.post("/update-stock-ai/{ticker}")`, docstring at :1305-1312 promises "1. Extract cash metrics (runway, burn rate, total cash) … AI output is validated against structured schema before DB update." It calls backend/app/services/gomes_ai_analyst.py:186 `analyze_document`, whose body is :217-222 `# TODO: Actual LLM call when implementing` / `# For now, return mock data` / `mock_output = self._generate_mock_analysis(ticker, document_text)`. The mock, :240-252: `total_cash=25_500_000.0 if has_cash_mention else None, quarterly_burn_rate=-2_100_000.0, cash_runway_months=12, … primary_catalyst="Q2 2026 Production Ramp", catalyst_date=date(2026, 6, 30), … insi …
- **Dopad na misi:** Breaks (5) mechanical/systematic and the "a fake number can cost real money" non-negotiable. This is a loaded gun: the endpoint is live on the router, documented in Swagger at http://localhost:8002/api/docs (printed by backend/run_server.py:18), and the ONLY thing stopping $25.5M of invented cash and a 12-month runway …
- **Návrh opravy:** Delete routes/gomes.py:1298-1412 and services/gomes_ai_analyst.py's mock outright — `/api/gomes/update-stock/{ticker}` already covers this with a real Gemini call. If the structured GomesAnalystOutput schema is worth keeping, keep only the Pydantic model and the prompt template, and make analyze_document raise NotImple …

**7. LIVE: 'Trh: GREEN' etc. aside, unknown values render as green 'safe' badges rather than unknown**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/stock-detail/SafetyBadge.tsx:140-142 — `const marketStatus: SafetyStatus =\n    marketAlert === 'RED' ? 'danger' :\n    marketAlert === 'YELLOW' ? 'warning' : 'safe';` — there is no 'unknown' branch and no 'ORANGE' branch even though frontend/src/types/index.ts:141 declares `export type MarketStatus = 'GREEN' | 'YELLOW' | 'ORANGE' | 'RED';`. Elsewhere the same file does it correctly (`:102 if (months === null) return 'unknown';`, `:113 if (!status) return 'unknown';`), so the omission is specific to the market light.
- **Dopad na misi:** An ORANGE (defensive) market renders as a green 'safe' checkmark. Combined with the hardcoded default this makes the pre-flight safety row structurally incapable of showing caution about market conditions — the layer whose stated job (SafetyBadge.tsx:7 '@fiduciary') is to stop him buying into a bad tape.
- **Návrh opravy:** Add explicit 'ORANGE' → 'warning' and null/undefined → 'unknown' branches; widen SafetyGaugeRowProps.marketAlert to the full MarketStatus union.

**8. LIVE: 'unrealized_pl' and 'market_value' silently return 0.0 when the price is unknown**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/models/portfolio.py:210-214 — `@property\n    def market_value(self) -> float:\n        """Current market value (shares * current_price)."""\n        if self.current_price is None:\n            return 0.0`. And :217-223 — `def unrealized_pl(self) -> float | None:\n        if self.avg_cost is None:\n            return None\n        if self.current_price is None:\n            return 0.0`. Same for `unrealized_pl_percent` at :226-231 (`if self.avg_cost == 0 or self.current_price is None: return 0.0`). The zero is then indistinguishable from a real flat position at InvestmentTerminal.tsx:943-946 (`{formatPercent(position.unrealized_pl_percent)}` / `{formatCurrency(position.unrealized …
- **Dopad na misi:** The code was carefully fixed to return None for unknown COST but still returns 0.0 for unknown PRICE — so a position whose quote failed shows 'P/L: 0,00 $ / +0.00%' (a claim that he is exactly break-even) and contributes 0 to Total AUM, silently understating the portfolio. Same class of defect the avg_cost fix was mean …
- **Návrh opravy:** Return None from all three properties when `current_price is None`, exclude those positions from `total_market_value` and surface a count ('N pozic bez ceny — hodnota portfolia je neúplná'). Update docs/PORTFOLIO_PL_CALCULATION.md:42-60, which currently documents the bug as the spec.

**9. LIVE: '30 WMA Status' column shows a valuation-band reading, not a moving average — and inverts on price**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:897 — `{/* 30 WMA Status */}` — the column rendering `position.trend_status` (:900-907). That value comes from InvestmentTerminal.tsx:209-217: `const getTrendStatus = (stock: Stock | undefined): 'BULLISH' | 'BEARISH' | 'NEUTRAL' => {\n  if (!stock || !stock.current_price || !stock.green_line || !stock.red_line) {\n    return 'NEUTRAL';\n  }\n  const priceRange = stock.red_line - stock.green_line;\n  ...\n  const position = (stock.current_price - stock.green_line) / priceRange;\n  if (position <= 0.4) return 'BULLISH';\n  if (position >= 0.7) return 'BEARISH';` — no moving average is computed anywhere in the codebase (grep for 'wma' in frontend/s …
- **Dopad na misi:** A stock trading near its Gomes red line — i.e. richly valued — is labelled 'BEARISH' under a header claiming to report its 30-week moving average trend; one near the green line is labelled 'BULLISH'. Two independent Gomes concepts (valuation zone and Weinstein trend) are conflated under the wrong name. Breaks point (5) …
- **Návrh opravy:** Rename the column to 'Cenová zóna' (which is what it computes), or compute a real 30-week MA server-side. Return null (rendered '—') instead of 'NEUTRAL' when inputs are missing.

**10. LIVE: prices and costs are always prefixed '$' regardless of the position's actual currency**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:819-821 — `<div className="text-sm font-bold text-text-primary font-mono">\n                ${position.current_price.toFixed(2)}\n              </div>` and :825-827 — `Cost: ${position.avg_cost.toFixed(2)}`. The literal `$` is hardcoded even though `position.currency` is available two lines later at :946 (`position.currency || 'USD'`) and the app supports USD/EUR/CZK/CAD/GBP/HKD/CHF (InvestmentTerminal.tsx CURRENCIES list at :990, importer `_extract_currency`, currency.py:44-56). Same pattern at StockDetail.tsx — `$` in AssetDetailModal.tsx:271, :518, :623, :778.
- **Dopad na misi:** An HKD position (rate 2.88, currency.py:50) priced at 12.40 HKD renders as '$12.40'. He reads a number four times larger than reality when eyeballing a position, and the mislabelled figure sits directly beside the correctly-labelled CZK totals. Violates 'a fake number can cost real money' and undermines point (5).
- **Návrh opravy:** Use the existing `formatCurrency(value, position.currency)` helper (already defined at InvestmentTerminal.tsx:104) instead of a bare `$` template literal, everywhere a per-position price is rendered.

**11. LIVE: 'Your last deposit brought retirement N days closer' celebrates a deposit that was never recorded**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:443-449 — `{daysSaved > 0 && (... 🎉 Tvůj poslední vklad {formatCurrency(monthlyContribution)} přiblížil důchod o <span className="font-bold">{daysSaved} dní</span>! ...)}`. `daysSaved` is not derived from any deposit record — InvestmentTerminal.tsx:329-340: `let valueWithoutLastContribution = currentValue - monthlyContribution;` ... `const daysSaved = (monthsWithout - months) * 30; // Approximate days`. It simply subtracts the hypothetical monthly figure from the current portfolio value. The app itself admits deposits are not tracked, 500 lines later: InvestmentTerminal.tsx:3948 `(skutečné vklady zatím nejsou trackované)`.
- **Dopad na misi:** Mission point (3) is 'motivate him'. Motivation built on a fabricated event is the worst possible failure mode for a man deciding whether the system is trustworthy with family money — the moment he notices he never made that deposit, every other number becomes suspect. Also contradicts point (5).
- **Návrh opravy:** Delete the block, or gate it behind real deposit records. If kept as a projection, phrase it as one: 'Každých 20 000 Kč zkrátí cestu o ~N dní' — a rule, not a claim about something that happened.

**12. LIVE: 'věk svobody' is computed from a hardcoded age 35 and rendered as a bare fact**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:91-95 — `const FREEDOM_GOAL_TARGET_CZK = 30_000_000;\nconst FREEDOM_GOAL_MONTHLY_CONTRIBUTION_CZK = 20_000;\nconst FREEDOM_GOAL_ANNUAL_RETURN = 0.15;\nconst FREEDOM_GOAL_TARGET_AGE = 50;\nconst FREEDOM_GOAL_CURRENT_AGE = 35;` passed at :3891-3895 and rendered at InvestmentTerminal.tsx:386-388 — `<div className="text-3xl font-black text-accent">{freedomAge}</div>\n<div className="text-xs text-text-secondary">věk svobody</div>` where `const freedomAge = currentAge + yearsToFreedom;` (:385). The 'these are manually-set targets' caveat exists only in a source comment (:90) and is never shown on screen. A second, contradictory target is hardcoded els …
- **Dopad na misi:** The Freedom tab is the app's motivation engine (point 3). It displays a specific age as though it were his, derived from a constant no one set, and the app simultaneously shows two different 'targets' (500 000 Kč in the header, 30 000 000 Kč in the Freedom tab) with no reconciliation. A motivational number that is sile …
- **Návrh opravy:** Make age/target/contribution user-settable and persisted; until then label the tile explicitly ('podle předpokladu: věk 35, 15 % p.a., 20 000 Kč/měs.'). Reconcile the 500k and 30M targets.

**13. LIVE: every enriched position is stamped inflection_status: 'UPCOMING' regardless of the real value**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:3053 — `inflection_status: 'UPCOMING',` — a string literal inside the `const enriched: EnrichedPosition = {...}` object built for every position (:3038-3055), while `stock?.inflection_status` is read correctly two hundred lines earlier for the allocation cap (:2685 `const inflectionStatus = stock?.inflection_status?.toUpperCase();`). The fabricated value is rendered at InvestmentTerminal.tsx:1526-1530 — `position.inflection_status === 'UPCOMING' ? 'bg-warning animate-pulse'` / `{position.inflection_status || 'PENDING'}`.
- **Dopad na misi:** 'UPCOMING' is the mid-tier bullish lifecycle state in the Gomes canon; 'WAIT_TIME' is the one that means 'dead money, do not hold'. Hardcoding UPCOMING on every position means a WAIT_TIME stock is never flagged as such on the position object. Breaks point (5) and the canon-fidelity requirement. Currently the render sit …
- **Návrh opravy:** `inflection_status: stock?.inflection_status ?? undefined,` and render 'NEZNÁMÁ FÁZE' when undefined rather than 'PENDING'.

**14. DEAD-BUT-PRESENT: AssetDetailModal invents a thesis paragraph and Floor/Base/Moon targets from price multipliers**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/AssetDetailModal.tsx:142-143 — `const thesisNarrative = stock?.thesis_narrative ??\n    'High-grade producer entering commercial stage. Operational leverage to commodity prices.';` rendered at :355 `<p className="text-xs text-text-primary leading-tight line-clamp-2">{thesisNarrative}</p>` under the header 'THE SETUP'. AssetDetailModal.tsx:147-149 — `const priceFloor = stock?.price_floor ?? currentPrice * 0.6;\n  const priceBase = stock?.price_base ?? currentPrice * 2.0;\n  const priceMoon = stock?.price_moon ?? currentPrice * 4.0;` rendered as hard dollar figures at :518 `<span className="text-negative font-mono font-bold text-right">${priceFloor.toFixed(2)}</span>` ( …
- **Dopad na misi:** A stock with no research attached displays a specific investment thesis about a mining company, a 'Liquidation Floor' at 60% of spot, a 'Moon Target' at 4× spot, and an 'insiders are HOLDING' badge — all invented. This is unreachable today (imported by nobody), but InvestmentTerminal.tsx:970 calls it the successor to t …
- **Návrh opravy:** Delete the file, or strip every `??` literal and render '—' / 'chybí analýza'. Do not leave a 65KB fabrication in the tree labelled as the replacement for the live component.

**15. DEAD-BUT-PRESENT: AssetDetailModal's R/R Ratio is a constant 7.5:1 shown in green whenever valuation data is missing**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/AssetDetailModal.tsx:632-634 — `{position.stock?.upside_to_ceiling_pct && position.stock?.risk_to_floor_pct\n                      ? `${(position.stock.upside_to_ceiling_pct / Math.abs(position.stock.risk_to_floor_pct)).toFixed(1)}:1`\n                      : `${((priceMoon / currentPrice - 1) * 100 / Math.abs((priceFloor / currentPrice - 1) * 100)).toFixed(1)}:1`}` under the label 'R/R Ratio'. With the fallbacks from :147-149 the else-branch is arithmetically constant: priceMoon/currentPrice = 4.0 → upside 300; priceFloor/currentPrice = 0.6 → |downside| 40; 300/40 = 7.5 → '7.5:1'. The colour logic at :627-631 renders `>= 3 ? 'text-positive'` — so it is always bright …
- **Dopad na misi:** Risk/reward is THE Gomes go/no-go number. Every stock lacking valuation data would display an identical, excellent, green 7.5:1 — a number that is not merely wrong but is the same wrong number for every ticker, which is exactly the kind of thing a busy user pattern-matches on rather than reads. Same reachability caveat …
- **Návrh opravy:** Render '—' when `upside_to_ceiling_pct` or `risk_to_floor_pct` is absent. Never derive a ratio from two placeholder multipliers of the same base price — it cancels to a constant by construction.

**16. DEAD-BUT-PRESENT: the Trim modal collects a sell price, previews the proceeds, and throws the price away**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/AssetDetailModal.tsx:86-97 — `const handleSaveTrim = async () => {\n    const sharesToTrim = parseFloat(trimShares);\n    ...\n    const remainingShares = position.shares_count - sharesToTrim;\n    await apiClient.updatePosition(position.id, { shares_count: remainingShares });` — `trimPrice` is never sent. Grep for `trimPrice` in the file returns only :84 (state), :802 (the input), :811/:816 (the preview `${(parseFloat(trimShares) * parseFloat(trimPrice)).toFixed(2)}` labelled 'Hodnota transakce'), and :839 (the disabled-check). The button is labelled :843 `{isSavingTrim ? 'Ukládám...' : 'Uložit transakci'}` — 'Save transaction'.
- **Dopad na misi:** The UI shows him the transaction value, calls the button 'Save transaction', and saves only a reduced share count — no trade record, no realised P/L, no proceeds. The Gomes doubling-rule/free-ride discipline depends on knowing what was actually taken off the table. This is the same failure family as the console.log Sel …
- **Návrh opravy:** Either POST a real sale (InvestmentLog already exists — backend/app/services/portfolio_reconciliation.py:258-266) or remove the price field and rename the button to 'Upravit počet kusů' so it stops claiming to record a transaction.

**17. Reconciliation service fabricates avg_cost=0 and sets current_price to the purchase price**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/services/portfolio_reconciliation.py:465 — `avg_cost = float(pos_data.get("avg_cost", 0))` — the exact NULL→0 laundering the mission warned about (when the key is absent it yields cost basis 0 → an infinite P/L%; when present-but-None from importer.py:634 `"avg_cost": cost,` it raises TypeError). :471 `cost_basis = shares * avg_cost` and :477-482 `avg_cost=avg_cost,\n            current_price=avg_cost,  # Will be updated by price refresh\n            cost_basis=cost_basis,\n            market_value=cost_basis,\n            unrealized_pl=0,\n            unrealized_pl_percent=0,`. Also :383 `new_cost = float(new_data.get("avg_cost", old_cost))` and :338/:358 `f"Previous: {position. …
- **Dopad na misi:** An armed landmine directly on the Degiro path the avg_cost fix was built for. `current_price=avg_cost` also fabricates a live quote from the purchase price, making unrealized_pl exactly 0 by construction. Note that `cost_basis`/`market_value`/`unrealized_pl`/`unrealized_pl_percent` are read-only @property on the model …
- **Návrh opravy:** Delete portfolio_reconciliation._add_position/_update_position or rewrite them on the same contract as routes/portfolio.py:519-531 (preserve None, never write computed properties, never set current_price from avg_cost). If the smart-upload endpoint is not used, remove it rather than leaving a second, wrong importer.

**18. Pressing "Analyze" never touches the internet — Gemini's Google Search grounding is hard-wired to None while the health endpoint advertises it as a live feature**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** core/prompts.py:260-262: `# Google Search Retrieval was deprecated by Gemini API` / `# Kept as None for interface compatibility` / `GOOGLE_SEARCH_CONFIG: Final[None] = None`. That constant is the default `tools` argument of the only model factory: core/analysis.py:212 `tools: Any | None = GOOGLE_SEARCH_CONFIG,` and :228-231 `if tools is not None: model_kwargs["tools"] = tools` — so tools is never attached. grep for `tools=` across backend/app returns no other hit. Yet routes/analysis.py:520-525 returns `"status": "healthy", "model": "gemini-3-pro-preview", "features": ["google_search", "aggressive_extraction", "gomes_rules"]`. The button itself: frontend AssetDetailModal.tsx:977 POSTs /api/i …
- **Dopad na misi:** This is mission point (1) verbatim — "he opens a stock, presses Analyze, and the app goes out to the internet, reads current news, and summarizes it" — and it does not happen. The app summarizes text he already had to find and paste himself, so it costs him time instead of saving it, against his MS-limited energy budge …
- **Návrh opravy:** Two independent fixes. (a) Delete `"google_search"` from routes/analysis.py:525 today — it is a false claim about a live system. (b) Wire the internet path that already exists and works: NewsMonitorService.fetch_news_polygon is proven live (HTTP 200, real articles) — call it from the analyze-ticker handler with the tic …

**19. No MCP client exists anywhere in the repo**  
*Oblast: Zdroje dat z internetu*

- **Důkaz:** `grep -rniE "\bmcp\b|modelcontextprotocol|mcp_server|fastmcp"` over backend/ and frontend/ (excluding node_modules and __pycache__) returns ZERO matches. backend/requirements.txt contains no mcp, no anthropic, no modelcontextprotocol — the full network-capable dependency list is `requests==2.32.3` (:26), `httpx==0.28.1` (:47, and its comment says "For testing FastAPI endpoints"), `yfinance==0.2.66` (:31) and `google-generativeai==0.8.3` (:19). `pip list` on the active interpreter confirms no mcp package installed.
- **Dopad na misi:** Mission point (1) explicitly names MCP servers as the preferred mechanism for real-time internet gathering. Stating this plainly so no one assumes a half-built MCP path exists: there is none, not even a stub. Any plan that assumes MCP is partially wired is wrong.
- **Návrh opravy:** Do not start with MCP. The cheaper, already-paid-for path is finding 3's Polygon news call, which is proven live on the existing key. If MCP is still wanted afterwards, it is a net-new integration (add the mcp client package, a server registry, and a tool-dispatch layer in the analyze path) — scope it as new work, not …

**20. KellyAllocatorWidget is never mounted — the "Kolik investovat?" screen does not exist in the running app**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** The component exists and is complete: frontend/src/components/KellyAllocatorWidget.tsx:25 `export const KellyAllocatorWidget: React.FC<KellyAllocatorWidgetProps> = ({` … :239 `export default KellyAllocatorWidget;`, with header text at :84-85 `Kelly Alokátor` / `Kolik investovat do každé pozice?`.
Proof it is unreachable — `grep -rn "KellyAllocatorWidget" frontend/ --include=*.ts --include=*.tsx --include=*.js --include=*.json` (excluding node_modules) returns FOUR hits, ALL inside KellyAllocatorWidget.tsx itself (lines 2, 13, 25, 239). Zero external importers, no lazy/dynamic import, no route entry.
The component tree is shallow and fully enumerable: frontend/src/App.tsx has exactly three im …
- **Dopad na misi:** Breaks (2) advise HOW MUCH to buy. The screen purpose-built to answer his question — take free capital, rank by conviction, output a CZK amount per ticker — is dead code. He instead has to read the CZK figure off a wide portfolio table column, which competes with the ≤3-actions / one-screen / 2-minute constraint.
- **Návrh opravy:** Do NOT simply mount it — its API contract is broken three separate ways (next finding) and it would render NaN. Fix the contract first, then mount it inside the portfolio tab next to DailyActionWidget (InvestmentTerminal.tsx:3548), passing the real portfolioId. Alternatively delete it and fold cash-deployment ranking i …

**21. No path anywhere sizes an ADD to a position he already owns**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** backend/app/services/daily_actions.py:126-158 — the held-position loop can only ever produce `_derisk_action`, `_doubling_action`, or `_rr_trim_action` (:149-155). All three are sells or trims.
The buy loop explicitly excludes holdings — :196-198: `for ticker, analysis in sorted(gomes_by_ticker.items()):` / `if ticker in held_tickers:` / `continue`.
So `_buy_action` (:344), the only function in the app that emits a share count for a purchase, is structurally unreachable for any ticker in the portfolio. A held stock whose R/R score rises far above its deserved score produces no action at all.
The only add-sizing he can see is the frontend gap figure (InvestmentTerminal.tsx:3093-3104 `let allo …
- **Dopad na misi:** Breaks (2) how much to buy. Gomes's method is about accumulating known names as they fall toward the green line — his §8a OFFICIAL list is mostly positions one already holds. The canon-faithful engine is wired to answer "how much to buy" only for names he does not own yet, which is the less common case.
- **Návrh opravy:** In the held-position loop, after the trim checks fail, call a new `_add_action` that reuses `_buy_action`'s guard + dual-source sizing but computes `budget_czk = min(portfolio_value_czk * max_position_pct/100 - position_value_czk, cash_czk)` — i.e. size the gap to the cap rather than the full cap. Give it an urgency ba …

### P2 — tření

**22. LIVE: the standard CSV import writes the purchase price into current_price when the export has no quote**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/routes/portfolio.py:526-527 — `current_price=imported_price if imported_price is not None else imported_cost,\n                        last_price_update=datetime.utcnow() if imported_price is not None else None,`. Trading-212 and XTB parsers never produce a price (backend/app/services/importer.py:200-206 and :456-463 emit only ticker/shares_count/avg_cost/currency), so every T212/XTB import lands with `current_price == avg_cost` and `last_price_update = None`. That renders as a real quote at InvestmentTerminal.tsx:819-821 and yields exactly `+0.00%` P/L (models/portfolio.py:226-231).
- **Dopad na misi:** He sees '$27.75' under 'Current Price' and '+0.00%' P/L for a position whose price has never been fetched — the purchase price wearing a market-price label. The `last_price_update = None` is honest, but no live component reads it (see the no-refresh finding), so the honesty never reaches the screen. The auto-refresh at …
- **Návrh opravy:** Leave `current_price` NULL when no quote was imported. The position row already handles null price correctly (InvestmentTerminal.tsx:833 renders '-').

**23. LIVE: available_cash silently becomes 0 Kč when the portfolio cash balance is unknown**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/services/daily_actions.py:109-111 — `if cash_czk is None:\n        warnings.append("⚠️ CHYBÍ ÚDAJE: hotovost portfolia není známa")\n        cash_czk = 0.0` → returned at :214 `available_cash_czk=round(cash_czk, 2),` and rendered as a definite figure at DailyActionWidget.tsx:142 — `BOXX / Cash: {formatCzk(data.available_cash_czk)}`. The warning is rendered separately at the bottom of the card (DailyActionWidget.tsx:227-235), visually detached from the pill.
- **Dopad na misi:** The most prominent pill on the daily screen asserts '0 Kč' cash when the truth is 'unknown'. Because the fabricated 0 also gates BUY sizing (daily_actions.py:383-390 `budget_czk = min(..., cash_czk)` then `if quantity < 1: return None`), an unknown cash balance silently suppresses every BUY recommendation while display …
- **Návrh opravy:** Make `available_cash_czk` nullable in the schema and render '—' plus the warning inline in the pill. Keep the buy-suppression, but say why.

**24. LIVE: three Merit Badges are hardcoded earned:false and can never be earned**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:584 — `earned: false, // Would track in DB when sale happens` (Weed Cutter), :601-602 — `description: 'Nakup přesně na supportu 30WMA',\n        earned: false, // Would track entry vs 30WMA` (Sniper), :631 — `earned: false, // Would track in DB` (Patient Investor). All three are counted in the denominator at :652 `const earnedCount = badges.filter(b => b.earned).length;` rendered at :666 `{earnedCount}/{badges.length} získáno`.
- **Dopad na misi:** The motivation system (point 3) permanently caps at 6/9 and shows three greyed goals the app is structurally incapable of awarding — including 'sell a stock scoring under 3', which is the single most important discipline behaviour the app should be reinforcing. Not a false number, but a false promise.
- **Návrh opravy:** Either implement them (a sale log exists: InvestmentLog, backend/app/services/portfolio_reconciliation.py:258) or drop them from the array so the count is honest.

**25. DEAD-BUT-PRESENT: client-side clock used as the data's 'last updated' timestamp**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/TopPicksWidget.tsx:53 — `setLastUpdate(new Date());` immediately after `const response = await apiClient.gomesTopPicks(...)` (:51), rendered at :131-133 `<div className="text-xs text-gray-500 mt-1">\n              Aktualizováno: {lastUpdate.toLocaleTimeString()}\n            </div>`. Worse in frontend/src/components/ActionCenter.tsx:65 — `setLastUpdated(data.last_updated || new Date().toISOString());` rendered at :156 `<p className="text-xs text-gray-400 mt-1">Aktualizováno: {new Date(lastUpdated).toLocaleTimeString()}</p>` — i.e. when the server declines to say how old its data is, the UI substitutes 'now'.
- **Dopad na misi:** Exactly the pattern the brief names: a freshness claim manufactured by the browser clock. ActionCenter's `|| new Date()` is the sharpest form — the absence of a server timestamp is converted into a claim of maximum freshness. Both are dead code today, but they are the house style being copied.
- **Návrh opravy:** Render the server's own timestamp or nothing. Replace `data.last_updated || new Date().toISOString()` with a nullable value rendered as 'stáří dat neznámé'.

**26. DEAD-BUT-PRESENT: GatekeeperShield's Weinstein Stage-4 lock is mathematically unreachable — wma_30 is 0.9 × the price**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/AssetDetailModal.tsx:237 — `wma_30: stock?.green_line ?? currentPrice * 0.9, // Use green_line as proxy for 30 WMA`, fed to frontend/src/hooks/useGatekeeperStatus.tsx:69-70 — `if (stock_price !== null && wma_30 !== null && stage_analysis === 4 && stock_price < wma_30) {\n    const percentBelow = (((wma_30 - stock_price) / wma_30) * 100).toFixed(1);`. With the fallback the test is `currentPrice < currentPrice * 0.9` — always false. When `green_line` IS present it is the Gomes valuation floor, not a 30-week moving average, so the message at :78 `Price ${percentBelow}% below falling 30 WMA` reports a fabricated percentage.
- **Dopad na misi:** The shield's stated priority-2 rule (GatekeeperShield.tsx:10 'LOCKED (Bear): price < wma_30 AND stage == 4 → Full blur') can never fire, and its explanatory number would be wrong if it did. This is the emotional-guardrail layer (point 4) with one of its two hard locks silently disabled. Unreachable today only because A …
- **Návrh opravy:** Pass a real 30-week MA or `null` (the hook already skips the rule on null). Never substitute an unrelated series and label it as the moving average.

**27. LIVE: the Gomes green/red lines are self-declared 'placeholder estimates' from 2026-01-17, marked verified=True, with the freshness fields never read**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** backend/app/trading/price_lines_data.py:14-16 — `TODO: These values should be verified against actual screenshots.\nThe values below are placeholder estimates that need manual review.` Yet all 20 entries carry `verified=True` and `extraction_date="2026-01-17"` (e.g. :49-56 `ticker="AEHR", green_line=9.50, red_line=90.00, ... verified=True, extraction_date="2026-01-17"`). Neither field is ever read: `get_price_lines_for_ticker` (backend/app/routes/stocks.py:38-46) returns only green/red/grey, and grep for `.verified` across backend/app finds no consumer. Reached via GET /api/stocks/enriched (stocks.py:196-231), used only by the dead PortfolioView.tsx:53 — the live terminal calls plain /api/st …
- **Dopad na misi:** These lines drive the price zone, the R/R score in daily_actions.py:329-332, the Buy Guard at daily_actions.py:358-370, and the '$9.50 / $90.00' figures rendered in TradingDeck.tsx:193/:223. They are seven-month-old hand-eyeballed estimates the file itself calls placeholders, presented to two decimal places with no dat …
- **Návrh opravy:** Surface `extraction_date` next to any rendered line, flip the false `verified=True` flags, and change `get_price_lines_for_ticker` to leave the DB values alone when the ticker is not in the table instead of nulling them.

**28. LIVE: 'Splaceno' / 'Zbývá uhradit' on the debt tab are derived from elapsed calendar months, not from payments**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:3992-3998 — `const startDate = new Date(d.date);\n                      const now = new Date();\n                      const monthsPassed = (now.getFullYear() - startDate.getFullYear()) * 12 +\n                                         (now.getMonth() - startDate.getMonth()) + 1;\n                      const paid = Math.max(0, monthsPassed) * parseFloat(d.monthlyPayment || '0');\n                      const remaining = Math.max(0, parseFloat(d.amount || '0') - paid);` — repeated at :4016-4022 and per-row at :4084-4090 / :4097-4101. No payment record exists; the underlying data is browser-only (`localStorage.getItem('akcion_debts')`, InvestmentTer …
- **Dopad na misi:** Assumes every instalment was paid, on time, and ignores interest entirely, then renders the result as a definite CZK figure ('Splaceno: X Kč'). The `+ 1` also credits a payment on the very first day of the start month. Family financial obligations displayed as tracked facts when nothing is tracked — and the whole datas …
- **Návrh opravy:** Label the tiles as estimates ('odhad podle splátkového kalendáře, bez úroků'), or record actual payments. Move akcion_debts/akcion_savings/akcion_shared_payments/akcion_tom_payments/akcion_misa_payments to the backend.

**29. The widget's on-screen "Kelly váhy podle skóre" reference table contradicts the backend, including telling him to hold 3% of a stock the backend classes as EXIT**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** frontend/src/components/KellyAllocatorWidget.tsx:20-23 — `// Kelly weights mapping (must match backend)` / `const KELLY_WEIGHTS: Record<number, number> = { 10: 20, 9: 15, 8: 12, 7: 10, 6: 8, 5: 5, 4: 3, 3: 2, 2: 1, 1: 0 };` rendered as chips at :152-163 (`{score}→{weight}%`).
Backend backend/app/services/kelly_allocator.py:80-92 `TARGET_WEIGHTS = { 10: 0.15, 9: 0.15, 8: 0.12, 7: 0.10, 6: 0.05, 5: 0.03, 4: 0.00, 3: 0.00, 2: 0.00, 1: 0.00, 0: 0.00 }` with the comments `4: 0.00, # EXIT - should not hold` and `3: 0.00, # EXIT - sell signal`.
Mismatches: score 10 → 20% shown vs 15% real; 6 → 8% vs 5%; 5 → 5% vs 3%; and critically 4 → 3% vs 0%, 3 → 2% vs 0%, 2 → 1% vs 0%. The comment on line 20 as …
- **Dopad na misi:** Breaks (4) stop emotional mistakes. A legend that says score 4 deserves 3% of the portfolio directly contradicts the engine's own EXIT verdict, and legends are what people remember after the numbers scroll away. It is latent rather than live only because the widget is dead.
- **Návrh opravy:** Delete the hardcoded `KELLY_WEIGHTS` const and render the table from whatever the backend returns (add the weight map to the allocation response, or drop the legend entirely). A duplicated constant with a "must match backend" comment and no test is guaranteed to drift again.

**30. The whole "how much to sell" detail path is orphaned, and would crash on any Degiro-imported position if it were mounted**  
*Oblast: Kolik koupit / prodat*

- **Důkaz:** UNREACHABLE — `grep -rn "AssetDetailModal" frontend/src` returns three hits: its own definition (AssetDetailModal.tsx:77), its own export (:1234), and a comment in InvestmentTerminal.tsx:970-971 that has it backwards — `// DEAD CODE: superseded by AssetDetailModal; exported only so strict TS` — marking `StockDetailModal` dead in favour of a component that is itself never rendered. The mounted detail modal is `StockDetail` (InvestmentTerminal.tsx:23, rendered :4615).
What is lost: AssetDetailModal.tsx:765 `prodejte akcie v hodnotě <span className="font-bold text-negative">{Math.abs(position.optimal_size).toLocaleString('cs-CZ')} Kč</span>.`, the OPTIMAL SIZE readout at :455-457, and the whole …
- **Dopad na misi:** Breaks (2) how much to sell. The reachable sell figures today are the PortfolioRow TRIM cell (InvestmentTerminal.tsx:852, CZK only) and DailyActionWidget (shares — the good one). The richer sell-sizing UI someone built is dark, and un-darkening it as-is would white-screen on the first Degiro position and still make him …
- **Návrh opravy:** Decide which detail modal survives — StockDetail is the mounted one — and delete the other two rather than leaving three. Whichever survives: type `avg_cost` as `number | null` and guard :779, and compute the share count for him (`Math.floor(Math.abs(optimal_size) / (price * fxRate))`) as a prefilled default in the tri …

### P3 — kosmetika

**31. DEAD-BUT-PRESENT: no-op action buttons and a hardcoded three-entry Score History in the superseded stock modal**  
*Oblast: Poctivost čísel a čerstvost dat*

- **Důkaz:** frontend/src/components/InvestmentTerminal.tsx:1679-1683 — `{/* Mock history - should come from score_history API */}\n            {[\n              { date: '2026-01-20', score: 8, status: 'IMPROVED' },\n              { date: '2026-01-10', score: 7, status: 'STABLE' },\n              { date: '2025-12-15', score: 6, status: 'DETERIORATED' },\n            ].map(...)` rendered under the heading 'Score History (Thesis Drift)' (:1676). Same component: :1499 `<button className="w-full py-2 bg-warning/30 ...">Take the Free Ride 🎰</button>`, :1653 'Add to Position', :1658 'Take Partial Profits (House Money)', :1664 'Close Entire Position' — four buttons with no onClick attribute at all. Also :999-10 …
- **Dopad na misi:** Not reachable, so no money is at risk today — but it is a fabricated three-point thesis-drift history with specific dates and scores, plus four dead trade buttons, sitting in the same 5 721-line file as the live terminal. A `backend/app/models/score_history.py` exists, so the real data is available. Keeping it invites …
- **Návrh opravy:** Delete StockDetailModal (InvestmentTerminal.tsx:963-1710) outright — it is exported only to satisfy noUnusedLocals per the comment at :970-971.

---

## Vyvráceno — nezkoumat znovu

Ověřovatel u nich našel, že tvrzení neplatí. Zapsáno proto, aby se k nim příště nikdo nevracel.

### P1 — blokuje misi

**1. Requirement #1 is implemented on the backend but unreachable from the UI. A live-internet news pipeline exists and works: backend\app\services\news_monitor.py fetches current articles from Polygon.io …**  
*Oblast: Tok „Analyzovat“*


### P2 — tření

**2. The transcript is sent in the URL query string rather than a request body — poor design, but it does not block the analysis. The server runs httptools (via uvicorn[standard]), not h11, so the 16 KB h1 …**  
*Oblast: Tok „Analyzovat“*


**3. Freshness data exists but is not exposed to the UI, and it is inconsistent across write paths. The Stock model has `updated_at` with `onupdate=func.now()` (backend/app/models/stock.py:73), and the mai …**  
*Oblast: Tok „Analyzovat“*


**4. Two secondary defects, not a missing cap: (a) the server-side KellyAllocatorService (backend/app/services/kelly_allocator.py:95, :215-218) caps allocations at a flat 15% of portfolio instead of the pe …**  
*Oblast: Emoční brzdy*


**5. If the /api/currency/rates call fails on its own while the rest of the API succeeds, InvestmentTerminal silently falls back to rate 1.0 for any currency outside {EUR, USD, CZK} (GBP, CHF, CAD, JPY, HK …**  
*Oblast: Poctivost čísel a čerstvost dat*


**6. The Gemini analysis path passes no grounding tools (GOOGLE_SEARCH_CONFIG is None and analysis.py's `if tools is not None` guard drops it), so /api/analyze/text reasons over transcript text plus live t …**  
*Oblast: Provozuschopnost a důvěra*


**7. The held-position gap analysis uses a different sizing doctrine than the buy path and can exceed the canon's ceiling. `GomesLogicEngine.calculate_max_allocation` (backend/app/core/gomes_logic.py:136-1 …**  
*Oblast: Kolik koupit / prodat*


### P3 — kosmetika

**8. GatekeeperShield's LOCKED state does blur its entire child subtree including the close and sell controls, with no dismiss affordance — but its only consumer, AssetDetailModal.tsx, is orphaned dead cod …**  
*Oblast: Emoční brzdy*


**9. `hideBuyButton` in frontend/src/hooks/useGatekeeperStatus.tsx is a dead struct field: it is set on all four code paths and read by nobody. It is redundant with `blur && showOverlay` (true in exactly t …**  
*Oblast: Emoční brzdy*


**10. The `?? currentPrice * 0.9` fallback at AssetDetailModal.tsx:237 is a dead, misleading constant: it can never satisfy `stock_price < wma_30`, but neither can `null`, so it changes no shield outcome an …**  
*Oblast: Emoční brzdy*


**11. `AssetDetailModal.tsx` — which collects a sale price, previews proceeds, then persists only the decremented share count — is orphaned dead code: nothing in the repo imports it, and `InvestmentTerminal …**  
*Oblast: Emoční brzdy*


**12. `_calculate_thesis_tracker` does contain a fail-open to 50.0 (master_signal.py:434-441), but that fabricated 50 cannot reach `buy_confidence` for any of the failure modes cited, because `generate_verd …**  
*Oblast: Emoční brzdy*


**13. `Position.market_value` returns 0.0 for a NULL `current_price` while `cost_basis`/`unrealized_pl` correctly return None — an inconsistent default. Its only unguarded consumer is `backend/app/routes/po …**  
*Oblast: Zdroje dat z internetu*


**14. `_default_weinstein_score` (backend/app/trading/master_signal.py:610-619) returns wma_30 equal to the passed price and combined_score 50.0 on missing-data/error paths, and that raw `wma_30` is seriali …**  
*Oblast: Zdroje dat z internetu*


**15. _get_price_from_massive (market_data.py:123-135) handles only HTTP 200 and logs nothing for any non-200, so a Polygon 429 is indistinguishable from "ticker not found" — unlike _get_price_from_finnhub …**  
*Oblast: Zdroje dat z internetu*


**16. news_monitor.py:255-257 substitutes `datetime.utcnow()` for a missing `published_utc` instead of `None`, fabricating a timestamp on a defensive branch that the sole upstream producer (fetch_news_polyg …**  
*Oblast: Zdroje dat z internetu*


**17. Not a caching/TTL problem. Two separate, smaller issues: (1) market_data.py:414 `return True` makes validate_ticker incapable of returning False for any format-valid string, so a hallucinated ticker p …**  
*Oblast: Zdroje dat z internetu*


**18. Two separate issues, neither as claimed. (1) `ThesisMonitor` (`backend/app/services/thesis_monitor.py`) and the `GomesAlert` table are dead duplicate code: the thesis-broken→CRITICAL→SELL guardrail IS …**  
*Oblast: Časování a automatika*


**19. `check_and_send_alerts` (backend/app/services/notifications.py:308-358) sends zero notifications, ever — not duplicates. It either returns early at line 329-331 because `NotificationService.from_env() …**  
*Oblast: Časování a automatika*


---

*Vygenerováno z auditu `wf_4c008ee3-eae`, 22. 8. 2026. Metodika: [GOMES_METHODOLOGY_CANON.md](GOMES_METHODOLOGY_CANON.md) · cíle a cesty: [EFFICIENT_INVESTING_PLAYBOOK.md](EFFICIENT_INVESTING_PLAYBOOK.md) · stav produktu: [AKCION_SPEC.md](AKCION_SPEC.md).*