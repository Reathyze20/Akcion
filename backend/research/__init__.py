"""
Offline research over Mark Gomes' "Priority Ideas" tracking sheet.

Not part of the service. `backend/app/` must never import from here; the
dependency runs the other way, so that the candidate and the reference
distribution are computed by one and the same code. See README.md.
"""
